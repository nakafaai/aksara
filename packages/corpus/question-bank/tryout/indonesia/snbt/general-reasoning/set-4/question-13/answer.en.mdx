export const metadata = {
  title: "Explanation for Problem 13",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2026-01-20",
};

#### Assign the grammatical roles

The target sentence has:

- subject: a young bird, `ckeeck?`
- object: a fish, `bluwpz`

#### Apply the stated pattern

The language uses `bo + object + mw-subject`. Put the object after `bo`, then attach `mw-` to the subject:

1. marker: `bo`
2. object: `bluwpz`
3. marked subject: `mwckeeck?`

The complete sentence is **`bo bluwpz mwckeeck?`**.
