

export const metadata = {
  title: "Equivalent Vectors",
    description: "Decide whether two vectors are equivalent from their components, and explain why changing the starting point does not change a vector's magnitude or direction.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2025-04-12",
  dateModified: "2026-09-12",
  subject: "Vector and Operations",
};

## Equal Vectors at Different Starting Points

Two vectors are equal, or represented by equivalent directed segments, <Highlight>when their components are equal</Highlight>. For nonzero vectors, this is the same as having equal magnitude and the same direction. For vectors <InlineMath math="\vec{a}" /> and <InlineMath math="\vec{b}" />, we write <InlineMath math="\vec{a} = \vec{b}" />.

Equivalent vectors can have different positions in a plane or space, but they maintain the same magnitude and direction.

<LineEquation
  cameraPosition={[7, 5, -6]}
  cameraTarget={[2, 1.5, 1]}
  title="Example of Equivalent Vectors"
  description="The purple and orange vectors have the same magnitude and direction, making them equivalent despite their different positions."
  data={[
    {
      points: [{ x: 0, y: 0, z: 0 }, { x: 3, y: 2, z: 1 }],
      color: "#7c3aed",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="v_1" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
    {
      points: [{ x: 1, y: 1, z: 1 }, { x: 4, y: 3, z: 2 }],
      color: "#ea580c",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="v_2" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
  ]}
/>

## Conditions for Equivalent Vectors

Two vectors are equal only when **both conditions hold at the same time**, so check both before you call them equal. A pair that satisfies only one of the two conditions is not equivalent.

Two nonzero vectors <InlineMath math="\overrightarrow{CD}" /> and <InlineMath math="\overrightarrow{PQ}" /> are equal if:

1. Both vectors have equal length: <InlineMath math="|\overrightarrow{CD}| = |\overrightarrow{PQ}|" />
2. Both vectors have the same direction

## Representation of Equivalent Vectors

Two vectors are equivalent when they share the **same length and the same direction**, so either one can stand in for the other. That replacement moves a vector to a convenient starting point without changing the vector itself.

### In Component Form

Vector equality means **every corresponding component is equal**. Check each axis, rather than only comparing the two magnitudes.

In a two-dimensional Cartesian plane, two vectors <InlineMath math="\vec{a}" /> and <InlineMath math="\vec{b}" /> are equivalent if:

<BlockMath math="\vec{a} = \vec{b} \quad\Longleftrightarrow\quad (a_1, a_2) = (b_1, b_2)" />

This equality holds exactly when <InlineMath math="a_1 = b_1" /> and <InlineMath math="a_2 = b_2" />.

In three-dimensional space, vectors <InlineMath math="\vec{a}" /> and <InlineMath math="\vec{b}" /> are equivalent if:

<BlockMath math="\begin{aligned} &amp;\vec{a} = \vec{b} \\ &amp;\Longleftrightarrow \\ &amp;(a_1, a_2, a_3) = (b_1, b_2, b_3) \end{aligned}" />

This equality holds exactly when <InlineMath math="a_1 = b_1" />, <InlineMath math="a_2 = b_2" />, and <InlineMath math="a_3 = b_3" />.

<LineEquation
  cameraPosition={[8, 5, -6]}
  cameraTarget={[2.5, 1.5, 1.5]}
  title="Equivalent Vectors in Component Form"
  description="Two vectors with the same components are always equivalent, even if they have different positions in space."
  data={[
    {
      points: [{ x: 0, y: 0, z: 0 }, { x: 2, y: 3, z: 1 }],
      color: "#c026d3",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="a=(2, 3, 1)" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
    {
      points: [{ x: 3, y: 0, z: 2 }, { x: 5, y: 3, z: 3 }],
      color: "#0891b2",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="b=(2, 3, 1)" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
  ]}
/>

### In Terms of Initial and Terminal Points

If vector <InlineMath math="\overrightarrow{AB}" /> has **initial point** <InlineMath math="A(x_1, y_1)" /> and terminal point <InlineMath math="B(x_2, y_2)" />, then the vector can be expressed as:

<BlockMath math="\overrightarrow{AB} = (x_2 - x_1, y_2 - y_1)" />

Two vectors <InlineMath math="\overrightarrow{AB}" /> and <InlineMath math="\overrightarrow{CD}" /> are equivalent if:

<BlockMath math="\begin{aligned} &amp;(x_2 - x_1, y_2 - y_1) \\ &amp;= (x_4 - x_3, y_4 - y_3) \end{aligned}" />

In this equation, <InlineMath math="C(x_3, y_3)" /> is the initial point and <InlineMath math="D(x_4, y_4)" /> is the terminal point of <InlineMath math="\overrightarrow{CD}" />.

## Rules That Follow from Vector Equality

Vector equality follows from **equality of every component**. Reflexivity compares a vector with itself, symmetry swaps the order of two equal vectors, and transitivity connects two equalities through a shared intermediate vector.

### Reflexive Property

Every vector is equivalent to **itself**.

<BlockMath math="\vec{a} = \vec{a}" />

### Symmetric Property

You may **exchange the left and right sides** of a vector equality.

If vector <InlineMath math="\vec{a}" /> is equivalent to vector <InlineMath math="\vec{b}" />, then vector <InlineMath math="\vec{b}" /> is also equivalent to vector <InlineMath math="\vec{a}" />.

<BlockMath math="\text{If } \vec{a} = \vec{b} \text{, then } \vec{b} = \vec{a}" />

### Transitive Property

Two equalities can be connected through the **same intermediate vector**.

If vector <InlineMath math="\vec{a}" /> is equivalent to vector <InlineMath math="\vec{b}" /> and vector <InlineMath math="\vec{b}" /> is equivalent to vector <InlineMath math="\vec{c}" />, then vector <InlineMath math="\vec{a}" /> is equivalent to vector <InlineMath math="\vec{c}" />.

<ContentStack>

  <BlockMath math="\text{If } \vec{a} = \vec{b} \text{ and } \vec{b} = \vec{c} \text{, then } \vec{a} = \vec{c}" />

  <LineEquation
    cameraPosition={[7, 5, -6]}
    cameraTarget={[2, 1.5, 1]}
    title="Transitive Property of Equivalent Vectors"
    description={
      <>
        Three equivalent vectors: if <InlineMath math="a = b" /> and{" "}
        <InlineMath math="b = c" />, then <InlineMath math="a = c" />.
      </>
    }
    data={[
      {
      points: [{ x: 0, y: 0, z: 0 }, { x: 2, y: 2, z: 0 }],
      color: "#d97706",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="a" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
      {
      points: [{ x: 1, y: 1, z: 2 }, { x: 3, y: 3, z: 2 }],
      color: "#059669",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="b" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
      {
      points: [{ x: 2, y: 0, z: 1 }, { x: 4, y: 2, z: 1 }],
      color: "#db2777",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="c" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
    ]}
  />
</ContentStack>

## Checking Equivalent Vectors from Their Components

Two vectors are equivalent when the **change in the first coordinate matches** and the change in the second coordinate matches. The examples below compare those changes directly.

### Equality from Matching Component Changes

Vector <InlineMath math="\overrightarrow{AB}" /> with <InlineMath math="A(2, 3)" /> and <InlineMath math="B(5, 7)" /> is equivalent to vector <InlineMath math="\overrightarrow{CD}" /> with <InlineMath math="C(1, 1)" /> and <InlineMath math="D(4, 5)" />.

Calculate **endpoint minus starting-point coordinates** for both arrows:

<MathContainer>
  <BlockMath math="\overrightarrow{AB} = (5-2, 7-3) = (3, 4)" />
  <BlockMath math="\overrightarrow{CD} = (4-1, 5-1) = (3, 4)" />
</MathContainer>

Since <InlineMath math="\overrightarrow{AB} = \overrightarrow{CD} = (3, 4)" />, vector <InlineMath math="\overrightarrow{AB}" /> is equivalent to vector <InlineMath math="\overrightarrow{CD}" />.

<LineEquation
  cameraPosition={[3, 4, 10]}
  cameraTarget={[3, 4, 0]}
  title={
    <>
      Vectors <InlineMath math="AB" /> and <InlineMath math="CD" />
    </>
  }
  description={
    <>
      Visualization of vectors <InlineMath math="AB(3,4,0)" /> and{" "}
      <InlineMath math="CD(3,4,0)" /> which are equivalent in space.
    </>
  }
  data={[
    {
      points: [{ x: 2, y: 3, z: 0 }, { x: 5, y: 7, z: 0 }],
      color: "#7c3aed",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="AB" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
    {
      points: [{ x: 1, y: 1, z: 0 }, { x: 4, y: 5, z: 0 }],
      color: "#ca8a04",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="CD" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
  ]}
/>

### Equivalent Vectors with Different Starting Points

Vector <InlineMath math="\overrightarrow{PQ}" /> with <InlineMath math="P(0, 0)" /> and <InlineMath math="Q(2, 2)" /> is equivalent to vector <InlineMath math="\overrightarrow{RS}" /> with <InlineMath math="R(3, 1)" /> and <InlineMath math="S(5, 3)" />.

Calculate **endpoint minus starting-point coordinates** for both arrows:

<MathContainer>
  <BlockMath math="\overrightarrow{PQ} = (2-0, 2-0) = (2, 2)" />
  <BlockMath math="\overrightarrow{RS} = (5-3, 3-1) = (2, 2)" />
</MathContainer>

Since <InlineMath math="\overrightarrow{PQ} = \overrightarrow{RS} = (2, 2)" />, vector <InlineMath math="\overrightarrow{PQ}" /> is equivalent to vector <InlineMath math="\overrightarrow{RS}" />.

<LineEquation
  cameraPosition={[2.5, 1.5, 8]}
  cameraTarget={[2.5, 1.5, 0]}
  title={
    <>
      Vectors <InlineMath math="PQ" /> and <InlineMath math="RS" />
    </>
  }
  description={
    <>
      Visualization of vectors <InlineMath math="PQ(2,2,0)" /> and{" "}
      <InlineMath math="RS(2,2,0)" /> which are equivalent in space.
    </>
  }
  data={[
    {
      points: [{ x: 0, y: 0, z: 0 }, { x: 2, y: 2, z: 0 }],
      color: "#0d9488",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="PQ" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
    {
      points: [{ x: 3, y: 1, z: 0 }, { x: 5, y: 3, z: 0 }],
      color: "#e11d48",
      cone: { position: "end", size: 0.25 },
      showPoints: false,
      smooth: false,
      labels: [{
        text: <InlineMath math="RS" />,
        progress: 1,
        offset: [0.2, 0.2, 0.2],
        anchorX: "left",
      }],
    },
  ]}
/>

## Why Starting Points Can Differ

Equivalent vectors have the same magnitude and direction even when their **starting points differ**. The same displacement can therefore be drawn at different positions without changing the vector. This is useful when comparing translations or moving a vector within a geometric construction.
