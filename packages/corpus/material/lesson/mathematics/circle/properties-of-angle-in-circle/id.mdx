export const metadata = {
  title: "Sifat Sudut dalam Lingkaran",
  description: "Gunakan diagram dan pembahasan untuk menghubungkan sudut keliling, teorema Thales, dan sudut berhadapan dalam segiempat tali busur.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2025-05-18",
  dateModified: "2026-09-12",
  subject: "Lingkaran",
};



## Sudut Keliling yang Menghadap Busur yang Sama

Sudut-sudut keliling yang menghadap **busur yang sama** memiliki besar yang sama jika titik sudutnya berada pada busur sisanya yang sama, atau dengan kata lain dalam segmen lingkaran yang sama.

<LineEquation
  title="Sudut Keliling Menghadap Busur yang Sama"
  description="Sudut keliling dalam segmen yang sama dan menghadap busur yang sama memiliki besar yang sama."
  data={[
    {
      smooth: false,
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="A" />, at: 0, offset: [0.5, 0, 0] },
        { text: <InlineMath math="B" />, at: 1, offset: [0.3, 0.3, 0] },
      ],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="C" />, at: 1, offset: [-0.5, -0.3, 0] }],
    },
    {
      points: [
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
        {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: false,
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        {
          x: 4 * Math.cos((3 * Math.PI) / 4),
          y: 4 * Math.sin((3 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0d9488",
      showPoints: true,
      labels: [{ text: <InlineMath math="D" />, at: 1, offset: [-0.5, 0.3, 0] }],
    },
    {
      points: [
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
        {
          x: 4 * Math.cos((3 * Math.PI) / 4),
          y: 4 * Math.sin((3 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0d9488",
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 46 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#d97706",
      showPoints: false,
      labels: [{ text: <>Busur <InlineMath math="AB" /></>, at: 22, offset: [0.3, 0.4, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 12]}
/>

**Sifat**: <InlineMath math="\angle ACB = \angle ADB" />

Kedua titik <InlineMath math="C" /> dan <InlineMath math="D" /> berada pada sisi yang sama terhadap tali busur <InlineMath math="AB" />. Karena itu, <InlineMath math="\angle ACB" /> dan <InlineMath math="\angle ADB" /> menghadap busur yang sama dari segmen yang sama dan memiliki besar yang sama.

## Sudut Pusat dan Sudut Keliling

[Sudut pusat](/id/materi/matematika/lingkaran/sudut-pusat-dan-sudut-keliling) besarnya **dua kali sudut keliling** yang menghadap busur yang sama.

<LineEquation
  title="Hubungan Sudut Pusat dan Sudut Keliling"
  description={
    <>
      Sudut pusat <InlineMath math="= 2 \times" /> sudut keliling untuk busur
      yang sama.
    </>
  }
  data={[
    {
      smooth: false,
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="O" />, at: 0, offset: [-0.5, -0.5, 0] },
        { text: <InlineMath math="A" />, at: 1, offset: [0.5, 0, 0] },
      ],
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="B" />, at: 1, offset: [0.3, 0.3, 0] }],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="C" />, at: 1, offset: [-0.5, -0.3, 0] }],
    },
    {
      points: [
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
        {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 106 }, (_, sampleIndex) => {
        const i = sampleIndex / 7;
        const angle = (((i * 45) / 15) * Math.PI) / 180;
        return {
          x: 1.2 * Math.cos(angle),
          y: 1.2 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#db2777",
      showPoints: false,
      labels: [{ text: <InlineMath math="2\theta" />, at: 49, offset: [0.5, 0.3, 0] }],
    },
    {
      smooth: false,
      points: (() => {
        const C = {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
        };
        const A = { x: 4, y: 0 };
        const B = {
          x: 4 * Math.cos(Math.PI / 4),
          y: 4 * Math.sin(Math.PI / 4),
        };

        // Calculate angle ACB at point C
        const angleCA = Math.atan2(A.y - C.y, A.x - C.x);
        const angleCB = Math.atan2(B.y - C.y, B.x - C.x);

        return Array.from({ length: 16 }, (_, i) => {
          const t = i / 15;
          const angle = angleCA + t * (angleCB - angleCA);
          return {
            x: C.x + 0.8 * Math.cos(angle),
            y: C.y + 0.8 * Math.sin(angle),
            z: 0,
          };
        });
      })(),
      color: "#d97706",
      showPoints: false,
      labels: [{ text: <InlineMath math="\theta" />, at: 7, offset: [0.5, 0.5, 0] }],
    },

]}
cameraPosition={[0, 0, 12]}
/>

**Sifat**: <InlineMath math="\angle AOB = 2 \times \angle ACB" />

## Sudut Keliling yang Menghadap Diameter

Setiap sudut keliling yang menghadap <Highlight>diameter lingkaran</Highlight> selalu merupakan sudut siku-siku (<InlineMath math="90^\circ" />).

<LineEquation
  title="Sudut Keliling Menghadap Diameter"
  description={<>Sudut keliling yang menghadap diameter selalu <InlineMath math="90^\circ" />.</>}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: -4, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="A" />, at: 0, offset: [-0.5, 0, 0] },
        { text: <InlineMath math="B" />, at: 1, offset: [0.5, 0, 0] },
      ],
    },
    {
      points: [
        { x: -4, y: 0, z: 0 },
        { x: 0, y: 4, z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="C" />, at: 1, offset: [0, 0.5, 0] }],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        { x: 0, y: 4, z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: false,
    },
    {
      points: [{ x: 0, y: 0, z: 0 }],
      color: "#d97706",
      showPoints: true,
      labels: [{ text: <InlineMath math="O" />, at: 0, offset: [0, -0.5, 0] }],
    },
    {
      smooth: false,
      points: (() => {
        const C = { x: 0, y: 4 };
        const A = { x: -4, y: 0 };
        const B = { x: 4, y: 0 };

        // Calculate angle ACB at point C (should be 90°)
        const angleCA = Math.atan2(A.y - C.y, A.x - C.x);
        const angleCB = Math.atan2(B.y - C.y, B.x - C.x);

        return Array.from({ length: 16 }, (_, i) => {
          const t = i / 15;
          const angle = angleCA + t * (angleCB - angleCA);
          return {
            x: C.x + 0.8 * Math.cos(angle),
            y: C.y + 0.8 * Math.sin(angle),
            z: 0,
          };
        });
      })(),
      color: "#db2777",
      showPoints: false,
      labels: [{ text: <InlineMath math="90^\circ" />, at: 7, offset: [-0.3, -0.5, 0] }],
    },
    {
      points: [
        { x: -4, y: 0, z: 0 },
        {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0d9488",
      showPoints: true,
      labels: [{ text: <InlineMath math="D" />, at: 1, offset: [-0.5, -0.3, 0] }],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0d9488",
      showPoints: false,
    },

]}
cameraPosition={[0, 0, 12]}
/>

**Sifat**: Jika <InlineMath math="AB" /> adalah diameter, maka <InlineMath math="\angle ACB = \angle ADB = 90^\circ" />

Ini dikenal sebagai **Teorema Thales**.

## Sudut dalam Segiempat Tali Busur

Segiempat tali busur memiliki empat titik sudut pada lingkaran. Dua **sudut yang berhadapan** menghadap dua busur yang bersama-sama membentuk satu lingkaran. Karena sudut keliling setengah besar busur yang dihadapi:

<BlockMath math="\alpha+\gamma=\frac{1}{2}\times360^\circ=180^\circ" />

<LineEquation
  title="Segiempat dengan Sudut Berhadapan"
  description="Dua sudut berhadapan menghadap busur yang melengkapi satu lingkaran."
  data={(() => {
    const point = (degrees) => ({ x: 4 * Math.cos(degrees * Math.PI / 180), y: 4 * Math.sin(degrees * Math.PI / 180), z: 0 });
    const A = point(0), B = point(80), C = point(200), D = point(280);
    const E = { x: A.x + 0.4 * (A.x - B.x), y: A.y + 0.4 * (A.y - B.y), z: 0 };
    const arc = (vertex, start, end) => {
      const a = Math.atan2(start.y - vertex.y, start.x - vertex.x);
      let delta = Math.atan2(end.y - vertex.y, end.x - vertex.x) - a;
      if (delta > Math.PI) delta -= 2 * Math.PI;
      if (delta < -Math.PI) delta += 2 * Math.PI;
      return Array.from({ length: 31 }, (_, i) => ({ x: vertex.x + 0.65 * Math.cos(a + delta * i / 30), y: vertex.y + 0.65 * Math.sin(a + delta * i / 30), z: 0 }));
    };
    return [
      { kind: "circle-outline", radius: 4, color: "#9333ea", showPoints: false },
      { points: [A, B, C, D, A], smooth: false, color: "#ea580c", showPoints: true, labels: [{ text: "A", at: 0, offset: [0.35, 0.2, 0] }, { text: "B", at: 1, offset: [0, 0.4, 0] }, { text: "C", at: 2, offset: [-0.4, 0, 0] }, { text: "D", at: 3, offset: [0, -0.4, 0] }] },
      { points: arc(A, D, B), smooth: false, color: "#0891b2", showPoints: false, labels: [{ text: <InlineMath math="\alpha" />, at: 15, offset: [0, -0.35, 0] }] },
      { points: arc(C, B, D), smooth: false, color: "#db2777", showPoints: false, labels: [{ text: <InlineMath math="\gamma" />, at: 15, offset: [0.3, 0.3, 0] }] },
    ];
  })()}
  cameraPosition={[0, 0, 12]}
/>

Pada gambar, sudut di <InlineMath math="A" /> sebesar <InlineMath math="100^\circ" /> dan sudut di <InlineMath math="C" /> sebesar <InlineMath math="80^\circ" />. Keduanya tidak harus siku-siku.

<BlockMath math="100^\circ+80^\circ=180^\circ" />

## Sudut Luar Sama dengan Sudut Dalam Berseberangan

Perpanjang sisi <InlineMath math="BA" /> ke <InlineMath math="E" />. Sudut luar <InlineMath math="DAE" /> **berpelurus** dengan sudut dalam di <InlineMath math="A" />. Sudut dalam di <InlineMath math="C" /> juga melengkapi sudut di <InlineMath math="A" /> menjadi <InlineMath math="180^\circ" />.

<BlockMath math="\varepsilon=180^\circ-\alpha=\gamma" />

<LineEquation
  title="Sudut Luar dan Sudut Dalam Berseberangan"
  description="Perpanjangan sisi membentuk sudut luar yang sama dengan sudut dalam berseberangan."
  data={(() => {
    const point = (degrees) => ({ x: 4 * Math.cos(degrees * Math.PI / 180), y: 4 * Math.sin(degrees * Math.PI / 180), z: 0 });
    const A = point(0), B = point(80), C = point(200), D = point(280);
    const E = { x: A.x + 0.4 * (A.x - B.x), y: A.y + 0.4 * (A.y - B.y), z: 0 };
    const arc = (vertex, start, end) => {
      const a = Math.atan2(start.y - vertex.y, start.x - vertex.x);
      let delta = Math.atan2(end.y - vertex.y, end.x - vertex.x) - a;
      if (delta > Math.PI) delta -= 2 * Math.PI;
      if (delta < -Math.PI) delta += 2 * Math.PI;
      return Array.from({ length: 31 }, (_, i) => ({ x: vertex.x + 0.65 * Math.cos(a + delta * i / 30), y: vertex.y + 0.65 * Math.sin(a + delta * i / 30), z: 0 }));
    };
    return [
      { kind: "circle-outline", radius: 4, color: "#9333ea", showPoints: false },
      { points: [A, B, C, D, A], smooth: false, color: "#ea580c", showPoints: true, labels: [{ text: "A", at: 0, offset: [0.35, 0.2, 0] }, { text: "B", at: 1, offset: [0, 0.4, 0] }, { text: "C", at: 2, offset: [-0.4, 0, 0] }, { text: "D", at: 3, offset: [0, -0.4, 0] }] },
      { points: arc(A, D, E), smooth: false, color: "#0891b2", showPoints: false, labels: [{ text: <InlineMath math="\varepsilon" />, at: 15, offset: [0, -0.35, 0] }] },
      { points: arc(C, B, D), smooth: false, color: "#db2777", showPoints: false, labels: [{ text: <InlineMath math="\gamma" />, at: 15, offset: [0.3, 0.3, 0] }] },
      { points: [A, E], smooth: false, color: "#0891b2", showPoints: true, labels: [{ text: "E", at: 1, offset: [0.35, 0, 0] }] },
    ];
  })()}
  cameraPosition={[0, 0, 12]}
/>

## Menghitung Sudut dengan Teorema Lingkaran

Setiap contoh memakai satu teorema lingkaran pada situasi yang sudah diberi label. Tentukan **sudut dan busur** yang dihubungkan teorema itu sebelum menghitung. Tuliskan hubungannya sebelum memasukkan angka, karena hubungan itu menunjukkan besaran yang dicari.

### Menentukan Besar Sudut

Pada lingkaran dengan pusat <InlineMath math="O" />, diketahui sudut pusat <InlineMath math="\angle AOB = 100^\circ" />. Tentukan besar sudut keliling <InlineMath math="\angle ACB" /> yang menghadap busur minor yang sama!

**Penyelesaian**:

Sudut keliling menghadap **busur yang sama**, sehingga besarnya setengah sudut pusat:

<MathContainer>
  <BlockMath math="\angle ACB = \frac{1}{2} \times \angle AOB" />
  <BlockMath math="\angle ACB = \frac{1}{2} \times 100^\circ" />
  <BlockMath math="\angle ACB = 50^\circ" />
</MathContainer>

### Mencari Sudut Berhadapan

Dalam segiempat tali busur <InlineMath math="ABCD" />, diketahui <InlineMath math="\angle A = 75^\circ" />. Tentukan besar <InlineMath math="\angle C" />!

**Penyelesaian**:

Dua **sudut berhadapan** dalam segiempat tali busur saling melengkapi menjadi sudut lurus:

<MathContainer>
  <BlockMath math="\angle A + \angle C = 180^\circ" />
  <BlockMath math="75^\circ + \angle C = 180^\circ" />
  <BlockMath math="\angle C = 180^\circ - 75^\circ" />
  <BlockMath math="\angle C = 105^\circ" />
</MathContainer>

### Menggunakan Teorema Thales

Titik <InlineMath math="C" /> terletak pada lingkaran dengan <InlineMath math="AB" /> sebagai diameter. Tentukan besar <InlineMath math="\angle ACB" />!

**Penyelesaian**:

Karena <InlineMath math="AB" /> adalah diameter dan <InlineMath math="C" /> terletak pada lingkaran, maka berdasarkan **Teorema Thales**:

<BlockMath math="\angle ACB = 90^\circ" />

## Latihan Soal

Tentukan **busur yang dihadapi** atau pasangan sudut berhadapan sebelum menghitung. Pada soal diameter, pastikan titik ketiga berbeda dari kedua ujung diameter agar segitiganya terbentuk.

1. Pada lingkaran dengan pusat <InlineMath math="O" />, sudut pusat <InlineMath math="\angle AOB = 140^\circ" /> menghadap busur minor <InlineMath math="AB" />. Titik <InlineMath math="C" /> dan <InlineMath math="D" /> berada pada busur mayor <InlineMath math="AB" />. Tentukan besar sudut <InlineMath math="\angle ACB" /> dan <InlineMath math="\angle ADB" />.

2. Dalam segiempat tali busur <InlineMath math="PQRS" />, diketahui:

   <MathContainer>
     <BlockMath math="\angle P = 85^\circ" />
     <BlockMath math="\angle Q = 110^\circ" />
   </MathContainer>

   Tentukan besar <InlineMath math="\angle R" /> dan <InlineMath math="\angle S" />!

3. Titik <InlineMath math="A" />, <InlineMath math="B" />, dan <InlineMath math="C" /> terletak pada lingkaran. Jika <InlineMath math="AB" /> adalah diameter dan <InlineMath math="BC = AC" />, tentukan besar <InlineMath math="\angle BAC" />!

4. Pada lingkaran, sudut keliling <InlineMath math="\angle APB = 35^\circ" />. Tentukan besar sudut pusat <InlineMath math="\angle AOB" /> yang menghadap busur yang sama!

5. Dalam segiempat tali busur <InlineMath math="KLMN" />, sudut luar di titik <InlineMath math="K" /> adalah <InlineMath math="65^\circ" />. Tentukan besar sudut dalam di titik <InlineMath math="M" />!

### Pembahasan

**Pembahasan 1**: Menentukan sudut keliling yang menghadap busur yang sama

<LineEquation
  title="Visualisasi"
  description={<>Sudut pusat <InlineMath math="\angle AOB = 140^\circ" /> dan sudut-sudut keliling menghadap busur <InlineMath math="AB" /> yang sama.</>}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="O" />, at: 0, offset: [-0.5, -0.5, 0] },
        { text: <InlineMath math="A" />, at: 1, offset: [0.5, 0, 0] },
      ],
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        {
          x: 4 * Math.cos((140 * Math.PI) / 180),
          y: 4 * Math.sin((140 * Math.PI) / 180),
          z: 0,
        },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="B" />, at: 1, offset: [-0.3, 0.5, 0] }],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="C" />, at: 1, offset: [-0.5, -0.3, 0] }],
    },
    {
      points: [
        {
          x: 4 * Math.cos((140 * Math.PI) / 180),
          y: 4 * Math.sin((140 * Math.PI) / 180),
          z: 0,
        },
        {
          x: 4 * Math.cos((5 * Math.PI) / 4),
          y: 4 * Math.sin((5 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: false,
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        {
          x: 4 * Math.cos((7 * Math.PI) / 4),
          y: 4 * Math.sin((7 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0d9488",
      showPoints: true,
      labels: [{ text: <InlineMath math="D" />, at: 1, offset: [0.3, -0.5, 0] }],
    },
    {
      points: [
        {
          x: 4 * Math.cos((140 * Math.PI) / 180),
          y: 4 * Math.sin((140 * Math.PI) / 180),
          z: 0,
        },
        {
          x: 4 * Math.cos((7 * Math.PI) / 4),
          y: 4 * Math.sin((7 * Math.PI) / 4),
          z: 0,
        },
      ],
      smooth: false,
      color: "#0d9488",
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 106 }, (_, sampleIndex) => {
        const i = sampleIndex / 7;
        const angle = (((i * 140) / 15) * Math.PI) / 180;
        return {
          x: 1.2 * Math.cos(angle),
          y: 1.2 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#db2777",
      showPoints: false,
      labels: [{ text: <InlineMath math="140^\circ" />, at: 49, offset: [0.3, 0.5, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 12]}
/>

**Penyelesaian**:

Menggunakan sifat hubungan sudut pusat dan sudut keliling:

<MathContainer>
  <BlockMath math="\text{Sudut keliling} = \frac{1}{2} \times \text{Sudut pusat}" />
  <BlockMath math="\angle ACB = \frac{1}{2} \times \angle AOB" />
  <BlockMath math="\angle ACB = \frac{1}{2} \times 140^\circ" />
  <BlockMath math="\angle ACB = 70^\circ" />
</MathContainer>

Karena sudut keliling yang menghadap busur yang sama besarnya sama:

<BlockMath math="\angle ADB = \angle ACB = 70^\circ" />

**Pembahasan 2**: Menentukan sudut-sudut dalam segiempat tali busur

**Penyelesaian**:

Dalam segiempat tali busur, jumlah sudut yang berhadapan adalah <InlineMath math="180^\circ" />.

Untuk sudut <InlineMath math="R" /> (berhadapan dengan <InlineMath math="P" />):

<MathContainer>
  <BlockMath math="\angle P + \angle R = 180^\circ" />
  <BlockMath math="85^\circ + \angle R = 180^\circ" />
  <BlockMath math="\angle R = 180^\circ - 85^\circ" />
  <BlockMath math="\angle R = 95^\circ" />
</MathContainer>

Untuk sudut <InlineMath math="S" /> (berhadapan dengan <InlineMath math="Q" />):

<MathContainer>
  <BlockMath math="\angle Q + \angle S = 180^\circ" />
  <BlockMath math="110^\circ + \angle S = 180^\circ" />
  <BlockMath math="\angle S = 180^\circ - 110^\circ" />
  <BlockMath math="\angle S = 70^\circ" />
</MathContainer>

**Pembahasan 3**: Menentukan sudut dalam segitiga sama kaki dengan diameter

<LineEquation
  title="Visualisasi"
  description={
    <>
      <InlineMath math="AB" /> adalah diameter,{" "}
      <InlineMath math="BC = AC" /> (segitiga sama kaki).
    </>
  }
  data={[
    {
      smooth: false,
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: -4, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="A" />, at: 0, offset: [-0.5, 0, 0] },
        { text: <InlineMath math="B" />, at: 1, offset: [0.5, 0, 0] },
      ],
    },
    {
      points: [
        { x: -4, y: 0, z: 0 },
        { x: 0, y: 4, z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="C" />, at: 1, offset: [0, 0.5, 0] }],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        { x: 0, y: 4, z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: false,
    },
    {
      points: [{ x: 0, y: 0, z: 0 }],
      color: "#d97706",
      showPoints: true,
      labels: [{ text: <InlineMath math="O" />, at: 0, offset: [0, -0.5, 0] }],
    },
    {
      smooth: false,
      points: (() => {
        const C = { x: 0, y: 4 };
        const A = { x: -4, y: 0 };
        const B = { x: 4, y: 0 };

        // Calculate angle ACB at point C (should be 90°)
        const angleCA = Math.atan2(A.y - C.y, A.x - C.x);
        const angleCB = Math.atan2(B.y - C.y, B.x - C.x);

        return Array.from({ length: 16 }, (_, i) => {
          const t = i / 15;
          const angle = angleCA + t * (angleCB - angleCA);
          return {
            x: C.x + 0.8 * Math.cos(angle),
            y: C.y + 0.8 * Math.sin(angle),
            z: 0,
          };
        });
      })(),
      color: "#db2777",
      showPoints: false,
      labels: [{ text: <InlineMath math="90^\circ" />, at: 7, offset: [-0.3, -0.5, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 12]}
/>

**Penyelesaian**:

Karena <InlineMath math="AB" /> adalah diameter, maka berdasarkan **Teorema Thales**:

<BlockMath math="\angle ACB = 90^\circ" />

Karena <InlineMath math="BC = AC" />, maka segitiga{" "}
<InlineMath math="ABC" /> adalah segitiga siku-siku sama kaki.

Dalam segitiga siku-siku sama kaki, kedua sudut alasnya sama besar:

<MathContainer>
  <BlockMath math="\angle BAC + \angle ABC + \angle ACB = 180^\circ" />
  <BlockMath math="\angle BAC + \angle ABC + 90^\circ = 180^\circ" />
  <BlockMath math="\angle BAC + \angle ABC = 90^\circ" />
</MathContainer>

Karena <InlineMath math="\angle BAC = \angle ABC" /> (sudut alas segitiga sama kaki):

<MathContainer>
  <BlockMath math="2 \times \angle BAC = 90^\circ" />
  <BlockMath math="\angle BAC = 45^\circ" />
</MathContainer>

**Pembahasan 4**: Menentukan sudut pusat dari sudut keliling

**Penyelesaian**:

Menggunakan hubungan sudut pusat dan sudut keliling:

<MathContainer>
  <BlockMath math="\text{Sudut pusat} = 2 \times \text{Sudut keliling}" />
  <BlockMath math="\angle AOB = 2 \times \angle APB" />
  <BlockMath math="\angle AOB = 2 \times 35^\circ" />
  <BlockMath math="\angle AOB = 70^\circ" />
</MathContainer>

**Pembahasan 5**: Menentukan sudut dalam dari sudut luar segiempat tali busur

**Penyelesaian**:

Dalam segiempat tali busur, sudut luar di suatu titik sama dengan sudut dalam di titik yang berseberangan.

Jika sudut luar di <InlineMath math="K" /> adalah <InlineMath math="65^\circ" />, maka:

<BlockMath math="\angle M = 65^\circ" />

Ini karena titik <InlineMath math="K" /> dan <InlineMath math="M" /> berseberangan dalam segiempat tali busur <InlineMath math="KLMN" />.
