
export const metadata = {
  title: "Central Angle and Inscribed Angle",
  description: "Connect central and inscribed angles through their intercepted arc, prove the relationship, and apply it to diameters and worked problems.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2025-05-18",
  dateModified: "2026-09-01",
  subject: "Circle",
};

## Definition of Central Angle

A central angle is an angle formed by two radii of a circle with the vertex located at the center of the circle. The sides of the central angle are radii that connect the center to points on the circle.

<LineEquation
  title="Central Angle"
  description="An angle whose vertex is located at the center of the circle."
  data={[
    {
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="O" />, at: 0, offset: [-0.5, -0.5, 0] },
        { text: <InlineMath math="A" />, at: 1, offset: [0.5, 0, 0] },
      ],
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="B" />, at: 1, offset: [0.3, 0.3, 0] }],
    },
    {
      points: Array.from({ length: 46 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 1.2 * Math.cos(angle),
          y: 1.2 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#0891b2",
      showPoints: false,
      labels: [{ text: <InlineMath math="\alpha" />, at: 22, offset: [0.3, 0.2, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 12]}
  showZAxis={false}
/>

In the figure above:

- Point <InlineMath math="O" /> is the center of the circle
- <InlineMath math="OA" /> and <InlineMath math="OB" /> are radii of the circle
- <InlineMath math="\angle AOB" /> is the central angle
- The measure of the central angle is denoted by <InlineMath math="\alpha" />

## Definition of Inscribed Angle

An inscribed angle is an angle formed by two chords with the vertex located on the circle. The sides of the inscribed angle are chords that connect the vertex to two other points on the circle.

<LineEquation
  title="Inscribed Angle"
  description="An angle whose vertex is located on the circle."
  data={[
    {
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="A" />, at: 0, offset: [0.5, 0, 0] },
        { text: <InlineMath math="C" />, at: 1, offset: [-0.5, -0.3, 0] },
      ],
    },
    {
      points: [
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="B" />, at: 0, offset: [0.3, 0.3, 0] }],
    },
    {
      points: [{ x: 0, y: 0, z: 0 }],
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="O" />, at: 0, offset: [0, -0.5, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 12]}
  showZAxis={false}
/>

In the figure above:

- Point <InlineMath math="C" /> is located on the circle
- <InlineMath math="CA" /> and <InlineMath math="CB" /> are chords
- <InlineMath math="\angle ACB" /> is the inscribed angle
- Point <InlineMath math="O" /> is the center of the circle

## Relationship Between Central Angle and Inscribed Angle

Central angles and inscribed angles that subtend the same arc have a special relationship. Observe this relationship.

<LineEquation
  title="Relationship Between Central Angle and Inscribed Angle"
  description="Central angle and inscribed angle that subtend the same arc."
  data={[
    {
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="O" />, at: 0, offset: [-0.5, -0.5, 0] },
        { text: <InlineMath math="A" />, at: 1, offset: [0.5, 0, 0] },
      ],
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="B" />, at: 1, offset: [0.3, 0.3, 0] }],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="C" />, at: 1, offset: [-0.5, -0.3, 0] }],
    },
    {
      points: [
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: false,
    },
    {
      points: Array.from({ length: 46 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#d97706",
      showPoints: false,
      labels: [{ text: <>Arc <InlineMath math="AB" /></>, at: 22, offset: [1.5, 0.5, 0] }],
    },
    {
      points: Array.from({ length: 46 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 1.2 * Math.cos(angle),
          y: 1.2 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#db2777",
      showPoints: false,
      labels: [{ text: <InlineMath math="\alpha" />, at: 22, offset: [0.3, 0.2, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 12]}
  showZAxis={false}
/>

### Theorem of Central Angle and Inscribed Angle Relationship

<BlockMath math="\text{Inscribed angle} = \frac{1}{2} \times \text{Central angle}" />

If a central angle and an inscribed angle subtend the same arc, then:

- <InlineMath math="\text{measure of inscribed angle} = \frac{1}{2} \times \text{measure of central angle}" />
- <InlineMath math="\text{measure of central angle} = 2 \times \text{measure of inscribed angle}" />

## Proof of Central Angle and Inscribed Angle Relationship

Draw the diameter through <InlineMath math="C" /> and the center <InlineMath math="O" />. The two resulting isosceles triangles connect the inscribed angle to the central angle.

<LineEquation
  title="Proof with Auxiliary Lines"
  description={
    <>
      The diameter through <InlineMath math="C" /> and <InlineMath math="O" />
      splits the figure into two isosceles triangles.
    </>
  }
  data={[
    {
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="O" />, at: 0, offset: [-0.5, -0.5, 0] },
        { text: <InlineMath math="A" />, at: 1, offset: [0.5, 0, 0] },
      ],
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4 * Math.cos(Math.PI / 2), y: 4 * Math.sin(Math.PI / 2), z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="B" />, at: 1, offset: [0.3, 0.3, 0] }],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="C" />, at: 1, offset: [-0.5, -0.3, 0] }],
    },
    {
      points: [
        { x: 4 * Math.cos(Math.PI / 2), y: 4 * Math.sin(Math.PI / 2), z: 0 },
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: false,
    },
    {
      points: [
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
        { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
      ],
      smooth: false,
      color: "#0d9488",
      showPoints: true,
      labels: [{ text: <InlineMath math="D" />, at: 1, offset: [0.8, 0.5, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 12]}
  showZAxis={false}
/>

**Proof steps:**

1. Construct line <InlineMath math="CD" /> that passes through point <InlineMath math="O" /> (center of the circle)
2. Because all four segments are radii, <InlineMath math="OA = OB = OC = OD" />
3. Triangles <InlineMath math="AOC" /> and <InlineMath math="BOC" /> are isosceles triangles
4. Let <InlineMath math="\angle ACO = x" /> and <InlineMath math="\angle BCO = y" />
5. Since they are isosceles triangles: <InlineMath math="\angle CAO = x" /> and <InlineMath math="\angle CBO = y" />
6. Exterior angles of triangles: <InlineMath math="\angle AOD = 2x" /> and <InlineMath math="\angle BOD = 2y" />
7. Therefore: <InlineMath math="\angle AOB = 2x + 2y = 2(x + y) = 2 \times \angle ACB" />

This construction proves the configuration shown, where <InlineMath math="C" /> lies on the arc opposite <InlineMath math="AB" />. Other positions can be split into the same isosceles-triangle cases. In every case, the two angles being compared must intercept the same arc.

## Properties of Central Angle and Inscribed Angle

1. **Inscribed Angle Subtending a Diameter**

   Every inscribed angle that subtends a diameter of a circle measures <InlineMath math="90^\circ" /> (right angle).

   <LineEquation
     title="Inscribed Angle Subtending a Diameter"
     description={<>Inscribed angle subtending a diameter is always <InlineMath math="90^\circ" />.</>}
     data={[
       {
         points: Array.from({ length: 361 }, (_, i) => {
           const angle = (i * Math.PI) / 180;
           return {
             x: 4 * Math.cos(angle),
             y: 4 * Math.sin(angle),
             z: 0,
           };
         }),
         color: "#9333ea",
         showPoints: false,
       },
       {
         points: [
           { x: -4, y: 0, z: 0 },
           { x: 4, y: 0, z: 0 },
         ],
         smooth: false,
         color: "#ea580c",
         showPoints: true,
         labels: [
           { text: <InlineMath math="A" />, at: 0, offset: [-0.5, 0, 0] },
           { text: <InlineMath math="B" />, at: 1, offset: [0.5, 0, 0] },
         ],
       },
       {
         points: [
           { x: -4, y: 0, z: 0 },
           { x: 0, y: 4, z: 0 },
         ],
         smooth: false,
         color: "#0891b2",
         showPoints: true,
         labels: [{ text: <InlineMath math="C" />, at: 1, offset: [0, 0.5, 0] }],
       },
       {
         points: [
           { x: 4, y: 0, z: 0 },
           { x: 0, y: 4, z: 0 },
         ],
         smooth: false,
         color: "#0891b2",
         showPoints: false,
       },
       {
         points: [{ x: 0, y: 0, z: 0 }],
         color: "#d97706",
         showPoints: true,
         labels: [{ text: <InlineMath math="O" />, at: 0, offset: [0, -0.5, 0] }],
       },
       {
         points: (() => {
           const C = { x: 0, y: 4 };
           const A = { x: -4, y: 0 };
           const B = { x: 4, y: 0 };

           // Calculate angle ACB at point C (should be 90°)
           const angleCA = Math.atan2(A.y - C.y, A.x - C.x);
           const angleCB = Math.atan2(B.y - C.y, B.x - C.x);

           return Array.from({ length: 16 }, (_, i) => {
             const t = i / 15;
             const angle = angleCA + t * (angleCB - angleCA);
             return {
               x: C.x + 0.8 * Math.cos(angle),
               y: C.y + 0.8 * Math.sin(angle),
               z: 0,
             };
           });
         })(),
         color: "#db2777",
         showPoints: false,
         labels: [{ text: <InlineMath math="90^\circ" />, at: 7, offset: [0.5, -0.5, 0] }],
       },
     ]}
     cameraPosition={[0, 0, 12]}
     showZAxis={false}
   />

2. **Inscribed Angles Subtending the Same Arc**

   <InlineMath math="\angle ACB = \angle ADB" />, both angles subtend the same
   arc <InlineMath math="AB" />.

   <LineEquation
     title="Inscribed Angles Subtending the Same Arc"
     description="Inscribed angles that subtend the same arc have equal measures."
     data={[
       {
         points: Array.from({ length: 361 }, (_, i) => {
           const angle = (i * Math.PI) / 180;
           return {
             x: 4 * Math.cos(angle),
             y: 4 * Math.sin(angle),
             z: 0,
           };
         }),
         color: "#9333ea",
         showPoints: false,
       },
       {
         points: [
           { x: 4, y: 0, z: 0 },
           { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
         ],
         smooth: false,
         color: "#ea580c",
         showPoints: true,
         labels: [
           { text: <InlineMath math="A" />, at: 0, offset: [0.5, 0, 0] },
           { text: <InlineMath math="B" />, at: 1, offset: [0.3, 0.3, 0] },
         ],
       },
       {
         points: [
           { x: 4, y: 0, z: 0 },
           { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
         ],
         smooth: false,
         color: "#0891b2",
         showPoints: true,
         labels: [{ text: <InlineMath math="C" />, at: 1, offset: [-0.5, -0.3, 0] }],
       },
       {
         points: [
           { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
           { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
         ],
         smooth: false,
         color: "#0891b2",
         showPoints: false,
       },
       {
         points: [
           { x: 4, y: 0, z: 0 },
           { x: 4 * Math.cos((3 * Math.PI) / 4), y: 4 * Math.sin((3 * Math.PI) / 4), z: 0 },
         ],
         smooth: false,
         color: "#0d9488",
         showPoints: true,
         labels: [{ text: <InlineMath math="D" />, at: 1, offset: [-0.5, 0.3, 0] }],
       },
       {
         points: [
           { x: 4 * Math.cos(Math.PI / 4), y: 4 * Math.sin(Math.PI / 4), z: 0 },
           { x: 4 * Math.cos((3 * Math.PI) / 4), y: 4 * Math.sin((3 * Math.PI) / 4), z: 0 },
         ],
         smooth: false,
         color: "#0d9488",
         showPoints: false,
       },
       {
         points: Array.from({ length: 46 }, (_, i) => {
           const angle = (i * Math.PI) / 180;
           return {
             x: 4 * Math.cos(angle),
             y: 4 * Math.sin(angle),
             z: 0,
           };
         }),
         color: "#d97706",
         showPoints: false,
         labels: [{ text: <>Arc <InlineMath math="AB" /></>, at: 22, offset: [1.5, 0.5, 0] }],
       },
     ]}
     cameraPosition={[0, 0, 12]}
     showZAxis={false}
   />

## Calculating Inscribed Angle

Given central angle <InlineMath math="\angle AOB = 80^\circ" />. Find the measure of inscribed angle <InlineMath math="\angle ACB" /> that subtends the same arc!

<LineEquation
  title="Visualization"
  description={<>Given central angle <InlineMath math="\angle AOB = 80^\circ" />, find inscribed angle <InlineMath math="\angle ACB" />.</>}
  data={[
    {
      points: Array.from({ length: 361 }, (_, i) => {
        const angle = (i * Math.PI) / 180;
        return {
          x: 4 * Math.cos(angle),
          y: 4 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#9333ea",
      showPoints: false,
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [
        { text: <InlineMath math="O" />, at: 0, offset: [-0.5, -0.5, 0] },
        { text: <InlineMath math="A" />, at: 1, offset: [0.5, 0, 0] },
      ],
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4 * Math.cos((80 * Math.PI) / 180), y: 4 * Math.sin((80 * Math.PI) / 180), z: 0 },
      ],
      smooth: false,
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="B" />, at: 1, offset: [0, 0.5, 0] }],
    },
    {
      points: [
        { x: 4, y: 0, z: 0 },
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="C" />, at: 1, offset: [-0.5, -0.3, 0] }],
    },
    {
      points: [
        { x: 4 * Math.cos((80 * Math.PI) / 180), y: 4 * Math.sin((80 * Math.PI) / 180), z: 0 },
        { x: 4 * Math.cos((5 * Math.PI) / 4), y: 4 * Math.sin((5 * Math.PI) / 4), z: 0 },
      ],
      smooth: false,
      color: "#0891b2",
      showPoints: false,
    },
    {
      points: Array.from({ length: 18 }, (_, i) => {
        const angle = ((i * 80) / 17) * Math.PI / 180;
        return {
          x: 1.2 * Math.cos(angle),
          y: 1.2 * Math.sin(angle),
          z: 0,
        };
      }),
      color: "#db2777",
      showPoints: false,
      labels: [{ text: <InlineMath math="80^\circ" />, at: 10, offset: [0.5, 0.3, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 12]}
  showZAxis={false}
/>

**Solution:**

<MathContainer>
  <BlockMath math="\angle ACB = \frac{1}{2} \times \angle AOB" />
  <BlockMath math="\angle ACB = \frac{1}{2} \times 80^\circ" />
  <BlockMath math="\angle ACB = 40^\circ" />
</MathContainer>

## Calculating Central Angle

Given inscribed angle <InlineMath math="\angle ACB = 35^\circ" />. Find the measure of central angle <InlineMath math="\angle AOB" /> that subtends the same arc!

**Solution:**

<MathContainer>
  <BlockMath math="\angle AOB = 2 \times \angle ACB" />
  <BlockMath math="\angle AOB = 2 \times 35^\circ" />
  <BlockMath math="\angle AOB = 70^\circ" />
</MathContainer>

## Practice Problems

1. If the central angle of a circle is <InlineMath math="120^\circ" />, what is the measure of the inscribed angle that subtends the same arc?

2. Inscribed angle <InlineMath math="\angle ACB = 45^\circ" />. Find the measure of central angle <InlineMath math="\angle AOB" /> that intercepts the same arc.

3. In a circle, inscribed angle <InlineMath math="\angle PQR" /> subtends a diameter. What is the measure of <InlineMath math="\angle PQR" />?

4. Two inscribed angles subtend the same arc. If one angle measures <InlineMath math="25^\circ" />, find the measure of the other angle!

### Worked Solutions

1. The inscribed angle subtends the same arc as the central angle, so its measure is half the central angle: <InlineMath math="\frac{1}{2} \times 120^\circ = 60^\circ" />.

2. The central angle and <InlineMath math="\angle ACB" /> intercept arc <InlineMath math="AB" />. The central angle is twice the inscribed angle:

   <BlockMath math="\angle AOB=2\angle ACB=2(45^\circ)=90^\circ" />

3. A diameter cuts off a semicircle, whose central angle is <InlineMath math="180^\circ" />. The inscribed angle is half of that angle, so <InlineMath math="\angle PQR = 90^\circ" />.

4. Inscribed angles that intercept the same arc have equal measures. The other angle is also <InlineMath math="25^\circ" />.

## Sources

- [Lehrerfortbildung BW](https://lehrerfortbildung-bw.de/u_matnatech/imp/gym/bp2016/fb2/m03_geo/1_hintergrund/2_gang/m9geo01_unterrichtsverlauf.pdf) presents classroom derivations of circle-angle theorems.
- [OpenStax Precalculus 2e](https://openstax.org/books/precalculus-2e/pages/5-1-angles) explains angle measure and arcs.
- [GeoGebra](https://www.geogebra.org/m/nbepfdzb) provides an interactive construction of the inscribed angle theorem.
