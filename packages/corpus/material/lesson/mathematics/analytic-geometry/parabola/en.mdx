export const metadata = {
  title: "Parabola",
  description: "Derive a parabola's equation from its vertex, focus, and directrix in worked examples.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2025-05-26",
  dateModified: "2026-09-12",
  subject: "Analytic Geometry",
};



## Parabolic Paths and Reflectors

If air resistance is neglected and gravity is treated as constant, the flight of a basketball follows a **parabolic** path. Water leaving a fountain at an angle follows the same model while those assumptions are reasonable.

Its shape is also useful in technology. Satellite dishes collect signals, spotlights direct light, and parabolic arches appear in bridges and modern architecture.

The key feature is the **reflection** property. Rays arriving parallel to the axis are reflected toward the **focus**. Conversely, rays emitted from the focus leave a parabolic reflector parallel to the axis. This is why parabolic shapes can collect or direct energy effectively.

<LineEquation
  title={<>Parabola <InlineMath math="y = ax^2" /> in Real Life</>}
  description={<>Visualization of parabola with focus at <InlineMath math="F(0, \frac{1}{4a})" /> and directrix <InlineMath math="y = -\frac{1}{4a}" />.</>}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 81 }, (_, pointIndex) => {
        const x = (pointIndex - 40) / 10;
        const y = 0.2 * x * x; // parabola y = 0.2x²
        return { x, y, z: 0 };
      }),
      color: "#9333ea",
      showPoints: false,
      labels: [{ text: "Parabola", at: 10, offset: [-0.5, 0.5, 0] }],
    },
    {
      points: [{ x: 0, y: 1.25, z: 0 }], // focus at (0, 1/(4*0.2)) = (0, 1.25)
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="F" />, at: 0, offset: [0.65, 0.5, 0] }],
    },
    {
      points: [{ x: 0, y: 0, z: 0 }], // vertex of parabola
      color: "#0891b2",
      showPoints: true,
      labels: [{ text: <InlineMath math="O" />, at: 0, offset: [-0.8, -0.8, 0] }],
    },
    {
      points: Array.from({ length: 2 }, (_, pointIndex) => {
        const x = -5 + pointIndex * 10;
        const y = -1.25; // directrix at y = -1/(4*0.2) = -1.25
        return { x, y, z: 0 };
      }),
      color: "#d97706",
      showPoints: false,
      smooth: false,
      labels: [{ text: "Directrix", at: 1, offset: [-1, -0.6, 0] }],
    },
    {
      points: [{ x: 0, y: 1.25, z: 0 }, { x: 2, y: 0.8, z: 0 }, { x: 2, y: -1.25, z: 0 }],
      smooth: false,
      color: "#0d9488",
      showPoints: true,
      labels: [
        { text: <InlineMath math="P" />, at: 1, offset: [0.4, 0.4, 0] },
        { text: <InlineMath math="Q" />, at: 2, offset: [0.4, -0.4, 0] },
      ],
    },
  ]}
  cameraPosition={[0, 0, 15]}
/>

## Parabola from a Fixed Point and a Fixed Line

A parabola consists of points at **equal distances from a focus and a directrix**. The focus is a fixed point outside the directrix, and the directrix is a fixed line. Distance to a line is measured **perpendicular** to that line.

In the first graph, take <InlineMath math="P(2,0.8)" />, focus <InlineMath math="F(0,1.25)" />, and perpendicular foot <InlineMath math="Q(2,-1.25)" />. The two segments show equal distances.

<BlockMath math="PF=\sqrt{(2-0)^2+(0.8-1.25)^2}=2.05,\qquad PQ=0.8-(-1.25)=2.05" />

To derive the general equation, place the focus at <InlineMath math="F(p,0)" /> and the directrix at <InlineMath math="x=-p" />, with <InlineMath math="p \ne 0" />. Equal distances for <InlineMath math="P(x,y)" /> give:

<BlockMath math="\sqrt{(x-p)^2+y^2}=|x+p|" />

Square the two nonnegative distances. Canceling matching terms leaves the horizontal standard form.

<BlockMath math="\begin{aligned}(x-p)^2+y^2&amp;=(x+p)^2\\x^2-2px+p^2+y^2&amp;=x^2+2px+p^2\\y^2&amp;=4px\end{aligned}" />

Swap the coordinate roles to put the focus at <InlineMath math="(0,p)" /> and the directrix at <InlineMath math="y=-p" />. The result is the vertical form <InlineMath math="x^2=4y" />.

## Standard Parabola Equations

Start with a parabola whose vertex is at the origin <InlineMath math="O(0,0)" />. Its opening direction produces four standard forms.

<LineEquation
  title={<>Four Standard Parabola Forms with Vertex <InlineMath math="O(0,0)" /></>}
  description={<>Standard parabolas <InlineMath math="x^2 = 4py" /> (vertical) and <InlineMath math="y^2 = 4px" /> (horizontal).</>}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 61 }, (_, i) => {
        const x = (i - 30) / 10; // x from -3 to 3
        const y = 0.25 * x * x; // parabola x² = 4py with p=1
        return { x, y, z: 0 };
      }),
      color: "#9333ea",
      showPoints: false,
      labels: [{ text: <InlineMath math="x^2=4y" />, at: 0, offset: [-1.3, 2, 0] }],
    },
    {
      smooth: false,
      points: Array.from({ length: 61 }, (_, i) => {
        const x = (i - 30) / 10; // x from -3 to 3
        const y = -0.25 * x * x; // parabola x² = -4py with p=1
        return { x, y, z: 0 };
      }),
      color: "#ea580c",
      showPoints: false,
      labels: [{ text: <InlineMath math="x^2=-4y" />, at: 60, offset: [1.3, -2, 0] }],
    },
    {
      smooth: false,
      points: Array.from({ length: 61 }, (_, i) => {
        const y = (i - 30) / 10; // y from -3 to 3
        const x = 0.25 * y * y; // parabola y² = 4px with p=1
        return { x, y, z: 0 };
      }),
      color: "#0891b2",
      showPoints: false,
      labels: [{ text: <InlineMath math="y^2=4x" />, at: 60, offset: [2.05, 1, 0] }],
    },
    {
      smooth: false,
      points: Array.from({ length: 61 }, (_, i) => {
        const y = (i - 30) / 10; // y from -3 to 3
        const x = -0.25 * y * y; // parabola y² = -4px with p=1
        return { x, y, z: 0 };
      }),
      color: "#0d9488",
      showPoints: false,
      labels: [{ text: <InlineMath math="y^2=-4x" />, at: 0, offset: [-2.05, -1, 0] }],
    },
    {
      points: [{ x: 0, y: 0, z: 0 }], // vertex of all parabolas at O(0,0)
      color: "#e11d48",
      showPoints: true,
      labels: [{ text: <InlineMath math="O" />, at: 0, offset: [-1, -1.1, 0] }],
    },
  ]}
  cameraPosition={[0, 0, 15]}
/>

The two aligned standard equations use a signed parameter <InlineMath math="p" />:

<MathContainer>
  <BlockMath math="y^2 = 4px \quad \text{(horizontal axis)}" />
  <BlockMath math="x^2 = 4py \quad \text{(vertical axis)}" />
</MathContainer>

If <InlineMath math="p > 0" />, the parabola opens right or up. If <InlineMath math="p < 0" />, it opens left or down. The **distance from the vertex to the focus** is <InlineMath math="|p|" />, and a larger <InlineMath math="|p|" /> produces a wider parabola.

For example, for the parabola <InlineMath math="y^2 = 4px" /> (opens rightward):

<MathContainer>
  <BlockMath math="\text{Vertex: } (0, 0)" />
  <BlockMath math="\text{Focus: } (p, 0)" />
  <BlockMath math="\text{Directrix: } x = -p" />
  <BlockMath math="\text{Axis of symmetry: } y = 0" />
</MathContainer>

## Parabola with Arbitrary Vertex

A parabola does not have to lie at the origin. When its vertex is <InlineMath math="(h, k)" />, we use a **shifted standard form**.

<LineEquation
  title={<>Parabola <InlineMath math="(x-h)^2 = 4p(y-k)" /> with Vertex <InlineMath math="(h,k)" /></>}
  description={<>Parabola with vertex <InlineMath math="V(2,1)" />, focus <InlineMath math="F(2,1.5)" />, and parameter <InlineMath math="p = 0.5" />.</>}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 121 }, (_, pointIndex) => {
        const x = -1 + pointIndex / 20;
        const h = 2,
          k = 1,
          p = 0.5;
        const y = k + (1 / (4 * p)) * Math.pow(x - h, 2); // parabola with vertex (h,k)
        return { x, y, z: 0 };
      }),
      color: "#4f46e5",
      showPoints: false,
      labels: [{ text: "Parabola", at: 10, offset: [-0.4, 0.4, 0] }],
    },
    {
      points: [{ x: 2, y: 1, z: 0 }], // vertex
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="V" />, at: 0, offset: [-0.8, 1.2, 0] }],
    },
    {
      points: [{ x: 2, y: 1.5, z: 0 }], // focus at (h, k+p) = (2, 1+0.5)
      color: "#65a30d",
      showPoints: true,
      labels: [{ text: <InlineMath math="F" />, at: 0, offset: [0.8, 0.5, 0] }],
    },
    {
      points: Array.from({ length: 2 }, (_, pointIndex) => {
        const x = -2 + pointIndex * 8;
        const y = 0.5; // directrix at y = k-p = 1-0.5 = 0.5
        return { x, y, z: 0 };
      }),
      color: "#d97706",
      showPoints: false,
      smooth: false,
      labels: [{ text: <InlineMath math="y=0.5" />, at: 1, offset: [-1, -0.5, 0] }],
    },
    {
      points: Array.from({ length: 2 }, (_, i) => {
        const yMin = -1;
        const yMax = 5;
        const x = 2; // axis of symmetry
        return { x, y: yMin + i * (yMax - yMin), z: 0 };
      }),
      color: "#db2777",
      showPoints: false,
      smooth: false,
      cone: { position: "both" },
      labels: [{ text: <InlineMath math="x=2" />, at: 1, offset: [0.5, 0.2, 0] }],
    },
  ]}
  cameraPosition={[2, 2, 12]}
  cameraTarget={[2, 2, 0]}
/>

General form of parabola equations with vertex at <InlineMath math="(h, k)" />:

<MathContainer>
  <BlockMath math="(y-k)^2 = 4p(x-h) \quad \text{(horizontal)}" />
  <BlockMath math="(x-h)^2 = 4p(y-k) \quad \text{(vertical)}" />
</MathContainer>

For vertical parabola <InlineMath math="(x-h)^2 = 4p(y-k)" />:

<MathContainer>
  <BlockMath math="\text{Vertex: } (h, k)" />
  <BlockMath math="\text{Focus: } (h, k+p)" />
  <BlockMath math="\text{Directrix: } y = k-p" />
  <BlockMath math="\text{Axis of symmetry: } x = h" />
</MathContainer>

For horizontal parabola <InlineMath math="(y-k)^2 = 4p(x-h)" />:

<MathContainer>
  <BlockMath math="\text{Vertex: } (h, k)" />
  <BlockMath math="\text{Focus: } (h+p, k)" />
  <BlockMath math="\text{Directrix: } x = h-p" />
  <BlockMath math="\text{Axis of symmetry: } y = k" />
</MathContainer>

> The sign of <InlineMath math="p" /> determines the opening direction. In the horizontal form, positive means right and negative means left. In the vertical form, positive means up and negative means down.

## Determining Parabola Elements

The equation of a parabola determines its orientation, vertex, focus, directrix, and axis of symmetry. Start by identifying the orientation:

- If variable <InlineMath math="x" /> is squared → **vertical** parabola (opens up/down)
- If variable <InlineMath math="y" /> is squared → **horizontal** parabola (opens left/right)

In the following example, we [complete the square](/en/subjects/mathematics/quadratic-function/quadratic-equation-perfect-square) to rewrite the equation in standard form. The standard form then gives the vertex, focus, directrix, and axis of symmetry.

**Example**: Given a parabola with equation <InlineMath math="x^2 - 4x - 8y + 12 = 0" />

The first step is to convert to standard form by completing the square:

<MathContainer>
  <BlockMath math="x^2 - 4x - 8y + 12 = 0" />
  <BlockMath math="x^2 - 4x = 8y - 12" />
  <BlockMath math="x^2 - 4x + 4 = 8y - 12 + 4" />
  <BlockMath math="(x-2)^2 = 8y - 8" />
  <BlockMath math="(x-2)^2 = 8(y-1)" />
</MathContainer>

From the form <InlineMath math="(x-2)^2 = 8(y-1)" />, we identify:

- <InlineMath math="h = 2" />, <InlineMath math="k = 1" />
- <InlineMath math="4p = 8" />, so <InlineMath math="p = 2" />

The parabola elements are:

<LineEquation
  title={<>Parabola Analysis <InlineMath math="(x-2)^2 = 8(y-1)" /></>}
  description={<>Identifying vertex <InlineMath math="V(2,1)" />, focus <InlineMath math="F(2,3)" />, directrix <InlineMath math="y = -1" />, and <InlineMath math="p = 2" />.</>}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 121 }, (_, pointIndex) => {
        const x = -4 + pointIndex / 10;
        const h = 2,
          k = 1,
          p = 2;
        const y = k + (1 / (4 * p)) * Math.pow(x - h, 2); // (x-h)² = 4p(y-k)
        return { x, y, z: 0 };
      }),
      color: "#7c3aed",
      showPoints: false,
      labels: [{ text: "Parabola", at: 29, offset: [0.5, 0.5, 0] }],
    },
    {
      points: [{ x: 2, y: 1, z: 0 }], // vertex (h,k)
      color: "#ea580c",
      showPoints: true,
      labels: [{ text: <InlineMath math="V" />, at: 0, offset: [-0.75, 1.2, 0] }],
    },
    {
      points: [{ x: 2, y: 3, z: 0 }], // focus (h, k+p) = (2, 1+2)
      color: "#65a30d",
      showPoints: true,
      labels: [{ text: <InlineMath math="F" />, at: 0, offset: [1, 0.5, 0] }],
    },
    {
      points: Array.from({ length: 2 }, (_, pointIndex) => {
        const x = -5 + pointIndex * 14;
        const y = -1; // directrix y = k-p = 1-2 = -1
        return { x, y, z: 0 };
      }),
      color: "#d97706",
      showPoints: false,
      smooth: false,
      labels: [{ text: <InlineMath math="y=-1" />, at: 1, offset: [-1, -0.5, 0] }],
    },
    {
      points: Array.from({ length: 2 }, (_, i) => {
        const yMin = -2;
        const yMax = 6;
        const x = 2; // axis of symmetry x = h = 2
        return { x, y: yMin + i * (yMax - yMin), z: 0 };
      }),
      color: "#db2777",
      showPoints: false,
      smooth: false,
      cone: { position: "both" },
      labels: [{ text: <InlineMath math="x=2" />, at: 1, offset: [0.5, 0.2, 0] }],
    },
  ]}
  cameraPosition={[1, 1.5, 15]}
  cameraTarget={[1, 1.5, 0]}
/>

From this analysis we obtain:

<MathContainer>
  <BlockMath math="\text{Vertex: } (2, 1)" />
  <BlockMath math="\text{Focus: } (2, 3)" />
  <BlockMath math="\text{Directrix: } y = -1" />
  <BlockMath math="\text{Axis of symmetry: } x = 2" />
</MathContainer>
  The parabola opens upward because <InlineMath math="p=2>0" />.

## Finding Parabola Equations Vertices and Foci

Two worked problems now connect the standard form to the vertex and focus.

**Problem 1**: Determine the equation of a parabola that has vertex at <InlineMath math="(1, -2)" /> and focus at <InlineMath math="(4, -2)" />.

**Solution**:

Since the vertex and focus have the same <InlineMath math="y" />-coordinate, this parabola is **horizontal**.

<MathContainer>
  <BlockMath math="\text{Vertex: } (h, k) = (1, -2)" />
  <BlockMath math="\text{Focus: } (h+p, k) = (4, -2)" />
</MathContainer>

From the focus condition, we get <InlineMath math="h + p = 4" />. Substituting <InlineMath math="h = 1" />:

<BlockMath math="1 + p = 4 \Rightarrow p = 3" />

The equation for a horizontal parabola is <InlineMath math="(y-k)^2 = 4p(x-h)" />:

<MathContainer>
  <BlockMath math="(y-(-2))^2 = 4(3)(x-1)" />
  <BlockMath math="(y+2)^2 = 12(x-1)" />
</MathContainer>

**Problem 2**: For parabola <InlineMath math="y^2 - 6y - 4x + 13 = 0" />. Determine the coordinates of the vertex and focus.

**Solution**:

Complete the square for variable <InlineMath math="y" />:

<MathContainer>
  <BlockMath math="y^2 - 6y - 4x + 13 = 0" />
  <BlockMath math="y^2 - 6y = 4x - 13" />
  <BlockMath math="y^2 - 6y + 9 = 4x - 13 + 9" />
  <BlockMath math="(y-3)^2 = 4x - 4" />
  <BlockMath math="(y-3)^2 = 4(x-1)" />
</MathContainer>

From the form <InlineMath math="(y-3)^2 = 4(x-1)" />, we identify:

<MathContainer>
  <BlockMath math="h = 1, \quad k = 3" />
  <BlockMath math="4p = 4 \Rightarrow p = 1" />
</MathContainer>

The calculation gives:

<MathContainer>
  <BlockMath math="\text{Vertex: } (h, k) = (1, 3)" />
  <BlockMath math="\begin{aligned} \text{Focus: } (h+p, k) &amp;= (1+1, 3) \\ &amp;= (2, 3) \end{aligned}" />
</MathContainer>
