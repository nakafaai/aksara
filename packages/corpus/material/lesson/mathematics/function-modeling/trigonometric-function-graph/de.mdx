
export const metadata = {
  title: "Graphen trigonometrischer Funktionen",
  description: "Lies Sinus-, Kosinus- und Tangensgraphen mithilfe interaktiver Darstellungen zu Bogenmaß, Amplitude, Periode und Transformationen.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2026-08-22",
  dateModified: "2026-09-12",
  subject: "Funktionen und ihre Modellierung",
};

## Periodische Muster mit Winkelfunktionen beschreiben

Bewegungen, die sich **regelmäßig wiederholen**, lassen sich mit Sinus oder Kosinus modellieren. Das Modell beschreibt eine ideale Welle und nicht jede Veränderung des Meerwassers.

Zum Lesen trigonometrischer Funktionsgraphen benötigen wir Winkel im Bogenmaß. Gradangaben sind im Alltag verbreitet, während das Bogenmaß die natürliche Einheit für Analysis und Funktionsgraphen ist.

## Grad und Bogenmaß umrechnen

Eine **vollständige Umdrehung** eines Kreises beträgt <InlineMath math="360^\circ" /> oder <InlineMath math="2\pi" /> Bogenmaß. Diese Beziehung liefert uns Umrechnungsformeln:

<MathContainer>

<BlockMath math="180^\circ = \pi \text{ Bogenmaß}" />

<BlockMath math="1^\circ = \frac{\pi}{180} \text{ Bogenmaß}" />

<BlockMath math="1 \text{ Radiant} = \frac{180^\circ}{\pi} \approx 57{,}3^\circ" />
</MathContainer>

### Umrechnungsbeispiele

**Grad in Bogenmaß** umrechnen:

<MathContainer>

<BlockMath math="90^\circ = 90 \times \frac{\pi}{180} = \frac{\pi}{2} \text{ Bogenmaß}" />

<BlockMath math="60^\circ = 60 \times \frac{\pi}{180} = \frac{\pi}{3} \text{ Bogenmaß}" />

<BlockMath math="45^\circ = 45 \times \frac{\pi}{180} = \frac{\pi}{4} \text{ Bogenmaß}" />
</MathContainer>

Bogenmaß in Grad umrechnen:

<MathContainer>

<BlockMath math="\frac{\pi}{6} \text{ Bogenmaß} = \frac{\pi}{6} \times \frac{180}{\pi} = 30^\circ" />

<BlockMath math="\frac{3\pi}{4} \text{ Bogenmaß} = \frac{3\pi}{4} \times \frac{180}{\pi} = 135^\circ" />
</MathContainer>

## Amplitude und Periode im Sinusgraphen

Zwei Größen beschreiben die Grundform eines periodischen Graphen besonders direkt: **Amplitude** und **Periode**. Die Amplitude gibt an, wie weit die Kurve über und unter ihre Mittellinie steigt, und die Periode gibt den horizontalen Abstand an, den eine vollständige Wiederholung braucht.

### Amplitude

Die Amplitude ist <Highlight>der größte vertikale Abstand von der Mittellinie</Highlight> einer Sinuskurve zu einem Hoch- oder Tiefpunkt. Bei den unverschobenen Funktionen <InlineMath math="y = A \sin x" /> und <InlineMath math="y = A \cos x" /> ist diese Mittellinie die <InlineMath math="x" />-Achse und die Amplitude beträgt <InlineMath math="|A|" />.

<LineEquation
  title="Amplitudenkonzept"
  description={<>Die Amplitude ist der senkrechte Abstand von der <InlineMath math="x" />-Achse zum Hochpunkt der Welle.</>}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 100 }, (_, i) => {
        const x = (i / 99) * 2 * Math.PI;
        return { x, y: 2 * Math.sin(x), z: 0 };
      }),
      color: "#0d9488",
      labels: [{ text: <>Amplitude = <InlineMath math="2" /></>, at: 25, offset: [0, 0.5, 0] }],
      showPoints: false,
    },
    {
      points: [
        { x: Math.PI / 2, y: 0, z: 0 },
        { x: Math.PI / 2, y: 2, z: 0 },
      ],
      smooth: false,
      color: "#e11d48",
      showPoints: false,
    },
  ]}
  showZAxis={false}
  cameraPosition={[Math.PI, 0, 10]}
  cameraTarget={[Math.PI, 0, 0]}
/>

### Periode

Die **Periode** ist die Länge des Intervalls für einen vollständigen Zyklus. Für <InlineMath math="y = \sin(Bx)" /> und <InlineMath math="y = \cos(Bx)" /> beträgt sie <InlineMath math="\frac{2\pi}{|B|}" />.

<LineEquation
  title="Periodenkonzept"
  description="Die Periode ist der horizontale Abstand für eine vollständige Welle."
  data={[
    {
      smooth: false,
      points: Array.from({ length: 100 }, (_, i) => {
        const x = (i / 99) * 4 * Math.PI;
        return { x, y: Math.sin(x), z: 0 };
      }),
      color: "#059669",
      labels: [{ text: <>Periode = <InlineMath math="2\pi" /></>, at: 25, offset: [0, 1.5, 0] }],
      showPoints: false,
    },
    {
      points: [
        { x: 0, y: -1.2, z: 0 },
        { x: 2 * Math.PI, y: -1.2, z: 0 },
      ],
      smooth: false,
      color: "#9333ea",
      lineWidth: 2,
      showPoints: false,
      cone: { position: "both" },
      labels: [
        {
          text: <InlineMath math="\longleftarrow\;2\pi\;\longrightarrow" />,
          at: 1,
          offset: [-3, -0.5, 0],

        },
      ],
    },
  ]}
  showZAxis={false}
  cameraPosition={[2*Math.PI, 0, 16]}
  cameraTarget={[2*Math.PI, 0, 0]}
/>

### Amplitude und Periode aus den Koeffizienten

Für die folgenden Formeln gelten <InlineMath math="A\neq0" /> und <InlineMath math="B\neq0" />. Die Amplitude verwendet den **Betrag**, die Grundperiode ist der kleinste positive Wiederholungsabstand. Ist bei Sinus oder Kosinus einer der Koeffizienten null, ist die Funktion konstant und hat keine kleinste positive Periode.

| Funktion | Amplitude | Periode |
| --- | --- | --- |
| <InlineMath math="A\sin(Bx)" /> | <InlineMath math="\lvert A\rvert" /> | <InlineMath math="2\pi/\lvert B\rvert" /> |
| <InlineMath math="A\cos(Bx)" /> | <InlineMath math="\lvert A\rvert" /> | <InlineMath math="2\pi/\lvert B\rvert" /> |
| <InlineMath math="A\tan(Bx)" /> | Nicht definiert | <InlineMath math="\pi/\lvert B\rvert" /> |

## Graph der Sinusfunktion

Die Funktion <InlineMath math="y = \sin x" /> ist periodisch mit der Periode <InlineMath math="2\pi" />. Ihr Graph wiederholt sich daher in jedem Intervall der Länge <InlineMath math="2\pi" />.

<LineEquation
  title={
    <>
      Graph von <InlineMath math="y = \sin x" />
    </>
  }
  description="Der Graph bildet eine Welle, die sich nach jeder Periode wiederholt."
  data={[
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: Math.sin(x), z: 0 };
      }),
      color: "#0d9488",
      labels: [{ text: <InlineMath math="y=\sin x" />, at: 228, offset: [1.5, 0.5, 0] }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
  cameraPosition={[0, 0, 15]}
/>

**Grapheneigenschaften**: <InlineMath math="y = \sin x" />

- **Periode**: <InlineMath math="2\pi" /> (der Graph wiederholt sich nach <InlineMath math="2\pi \text{ Einheiten}" />)
- **Amplitude**: <InlineMath math="1" /> (Maximalwert minus Minimalwert, dann geteilt durch <InlineMath math="2" />)
- **Definitionsbereich**: Alle reellen Zahlen
- **Wertebereich**: <InlineMath math="[-1, 1]" />
- Schnittpunkte mit der <InlineMath math="x" />-Achse: <InlineMath math="x = n\pi" />, wobei <InlineMath math="n" /> eine ganze Zahl ist
- **Maximalwert**: <InlineMath math="1" /> bei <InlineMath math="x = \frac{\pi}{2} + 2n\pi" />
- **Minimalwert**: <InlineMath math="-1" /> bei <InlineMath math="x = \frac{3\pi}{2} + 2n\pi" />

## Graph der Kosinusfunktion

Die Funktion <InlineMath math="y = \cos x" /> hat eine sinusähnliche Form, ist jedoch um <InlineMath math="\frac{\pi}{2}" /> nach links verschoben.

<LineEquation
  title={
    <>
      Graph von <InlineMath math="y = \cos x" />
    </>
  }
  description={<>Vergleiche den Graphen mit <InlineMath math="\sin" />. Die Form ist gleich, aber horizontal verschoben.</>}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: Math.cos(x), z: 0 };
      }),
      color: "#e11d48",
      labels: [{ text: <InlineMath math="y=\cos x" />, at: 228, offset: [0.3, 1.5, 0] }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
  cameraPosition={[0, 0, 15]}
/>

**Grapheneigenschaften**: <InlineMath math="y = \cos x" />

- **Periode**: <InlineMath math="2\pi" />
- **Amplitude**: <InlineMath math="1" />
- **Definitionsbereich**: Alle reellen Zahlen
- **Wertebereich**: <InlineMath math="[-1, 1]" />
- Schnittpunkte mit der <InlineMath math="x" />-Achse: <InlineMath math="x = \frac{\pi}{2} + n\pi" />
- **Maximalwert**: <InlineMath math="1" /> bei <InlineMath math="x = 2n\pi" />
- **Minimalwert**: <InlineMath math="-1" /> bei <InlineMath math="x = \pi + 2n\pi" />

### Sinus und Kosinus vergleichen

Beide Kurven wiederholen sich nach einer vollen Umdrehung und unterscheiden sich um eine **waagerechte Verschiebung**. Vergleiche die Hochpunkte und Nullstellen beider Kurven in derselben Ansicht.

<LineEquation
  title={<>Vergleich der Graphen von <InlineMath math="\sin" /> und <InlineMath math="\cos" /></>}
  description={
    <>
      Es gilt <InlineMath math="\cos x = \sin(x + \frac{\pi}{2})" />.
    </>
  }
  data={[
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: Math.sin(x), z: 0 };
      }),
      color: "#0d9488",
      labels: [{ text: <InlineMath math="y=\sin x" />, at: 228, offset: [1.5, -1.5, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: Math.cos(x), z: 0 };
      }),
      color: "#e11d48",
      labels: [{ text: <InlineMath math="y=\cos x" />, at: 328, offset: [1.5, 2.5, 0] }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
  cameraPosition={[0, 0, 15]}
/>

## Graph der Tangensfunktion

Die Funktion <InlineMath math="y = \tan x" /> unterscheidet sich von <InlineMath math="\sin" /> und <InlineMath math="\cos" /> durch vertikale Asymptoten.

<LineEquation
  title={
    <>
      Graph von <InlineMath math="y = \tan x" />
    </>
  }
  description="Die roten vertikalen Linien markieren die Asymptoten."
  data={[
    // Tangensfunktion
    ...Array.from({ length: 3 }, (_, periodIndex) => {
      const offset = (periodIndex - 1) * Math.PI;
      return {
        smooth: false,
        points: (() => {
          const parameters = Array.from({ length: 321 }, (_, i) => (i - 160) / 64);
          parameters.push(0.02885536458803665);
          return parameters
            .sort((a, b) => a - b)
            .map((t) => ({
              x: offset + Math.atan(Math.sinh(t)),
              y: Math.sinh(t),
              z: 0,
            }));
        })(),
        color: "#059669",
        labels:
          periodIndex === 1
            ? [{ text: <InlineMath math="y=\tan x" />, at: 162, offset: [2, 0.5, 0] }]
            : [],
        showPoints: false,
      };
    }),
    // Vertikale Asymptoten
    ...Array.from({ length: 4 }, (_, i) => {
      const x = (i - 1) * Math.PI - Math.PI / 2;
      return {
        points: [{ x, y: -6, z: 0 }, { x, y: 5, z: 0 }, { x, y: 6, z: 0 }],
        smooth: false,
        color: "#e11d48",
        showPoints: false,
        labels:
          i === 2 ? [{ text: <InlineMath math="x=\frac{\pi}{2}" />, at: 1, offset: [1, 0.5, 0] }] : [],
      };
    }),
  ]}
  showZAxis={false}
  cameraPosition={[0, 0, 15]}
/>

**Grapheneigenschaften**: <InlineMath math="y = \tan x" />

- **Periode**: <InlineMath math="\pi" /> (kürzer als bei <InlineMath math="\sin" /> und <InlineMath math="\cos" />)
- **Amplitude**: nicht definiert
- **Definitionsbereich**: <InlineMath math="x \neq \frac{\pi}{2} + n\pi" />
- **Wertebereich**: Alle reellen Zahlen
- **Vertikale Asymptoten**: <InlineMath math="x = \frac{\pi}{2} + n\pi" />
- Schnittpunkte mit der <InlineMath math="x" />-Achse: <InlineMath math="x = n\pi" />

## Transformationen trigonometrischer Funktionen

Änderungen von **Amplitude, Periode und Mittellinie** lassen sich an den Koeffizienten erkennen. Vergleiche beim Ändern eines Koeffizienten die Hochpunkte und Nullstellen, um seine Wirkung zu sehen.

### Amplitudenänderungen

Für <InlineMath math="y = A \sin x" /> beträgt die **Amplitude** <InlineMath math="|A|" />.

<LineEquation
  title="Wirkung der Amplitude"
  description={
    <>
      Vergleiche, wie der Wert von <InlineMath math="A" /> die Wellenhöhe verändert.
    </>
  }
  data={[
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: Math.sin(x), z: 0 };
      }),
      color: "#0d9488",
      labels: [{ text: <InlineMath math="y=\sin x" />, at: 228, offset: [1.5, -2.5, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: 2 * Math.sin(x), z: 0 };
      }),
      color: "#e11d48",
      labels: [{ text: <InlineMath math="y=2\sin x" />, at: 228, offset: [1.25, 1.5, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 278 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 277) * 4 * Math.PI;
        return { x, y: 0.5 * Math.sin(x), z: 0 };
      }),
      color: "#059669",
      labels: [{ text: <InlineMath math="y=0{,}5\sin x" />, at: 114, offset: [1, 2.5, 0] }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
  cameraPosition={[0, 0, 15]}
/>

### Periodenänderungen

Die Funktion <InlineMath math="y = \sin(Bx)" /> hat die **Periode** <InlineMath math="\frac{2\pi}{|B|}" />.

<LineEquation
  title="Wirkung der Periode"
  description={
    <>
      Der Wert <InlineMath math="B" /> bestimmt, wie schnell sich der Graph wiederholt.
    </>
  }
  data={[
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: Math.sin(x), z: 0 };
      }),
      color: "#0d9488",
      labels: [{ text: <InlineMath math="y=\sin x" />, at: 228, offset: [1.5, -2.5, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 1109 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 1108) * 4 * Math.PI;
        return { x, y: Math.sin(2 * x), z: 0 };
      }),
      color: "#e11d48",
      labels: [{ text: <InlineMath math="y=\sin 2x" />, at: 596, offset: [1.25, 1.5, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 278 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 277) * 4 * Math.PI;
        return { x, y: Math.sin(0.5 * x), z: 0 };
      }),
      color: "#059669",
      labels: [{ text: <InlineMath math="y=\sin 0{,}5x" />, at: 114, offset: [1, 2.5, 0] }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
  cameraPosition={[0, 0, 15]}
/>

### Vertikale und horizontale Verschiebungen

Diese Form trennt **Verschiebungen innerhalb und außerhalb der Funktion**. Die Mittellinie lautet <InlineMath math="y=D" /> und muss nicht mit der waagerechten Achse zusammenfallen.

Allgemeine Form:

<BlockMath math="y = A \sin(B(x - C)) + D" />

- <InlineMath math="|A|" />: Amplitude
- <InlineMath math="B" />: Bestimmt die Periode (
  <InlineMath math="\text{Periode} = \frac{2\pi}{|B|}" />)
- <InlineMath math="C" />: Horizontale Verschiebung (Phase)
- <InlineMath math="D" />: Vertikale Verschiebung

Die folgende Darstellung verbindet Amplitude, horizontale Verschiebung und vertikale Verschiebung:

<LineEquation
  title="Vollständige Transformation"
  description={
    <>
      Der Graph von <InlineMath math="y = 2\sin(x - \frac{\pi}{4}) + 1" /> zeigt
      alle drei Transformationen.
    </>
  }
  data={[
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: Math.sin(x), z: 0 };
      }),
      color: "#0d9488",
      labels: [{ text: <InlineMath math="y=\sin x" />, at: 228, offset: [0.5, -2, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 555 }, (_, pointIndex) => {
        const x = -2 * Math.PI + (pointIndex / 554) * 4 * Math.PI;
        return { x, y: 2 * Math.sin(x - Math.PI / 4) + 1, z: 0 };
      }),
      color: "#e11d48",
      labels: [
        { text: <InlineMath math="y=2\sin(x-\frac{\pi}{4})+1" />, at: 228, offset: [0.5, 1.5, 0] },
      ],
      showPoints: false,
    },
  ]}
  showZAxis={false}
  cameraPosition={[0, 0, 15]}
/>

## Übungen

Die fünf Aufgaben unten wenden die Regeln für Periode, Amplitude und Verschiebung auf Graphen an, die du selbst skizzierst. Rechne jede Aufgabe vor der Lösung und vergleiche danach deine Skizzen und Gleichungen mit den angegebenen Antworten.

1. Rechne die folgenden Winkel um:

   - <InlineMath math="120^\circ" /> in Bogenmaß
   - <InlineMath math="\frac{5\pi}{6}" /> Bogenmaß in Grad

2. Bestimme Periode und Amplitude von:

   - <InlineMath math="y = 3 \sin(2x)" />
   - <InlineMath math="y = -2 \cos(\frac{x}{3})" />

3. Skizziere den Graphen von <InlineMath math="y = 2 \sin(x + \frac{\pi}{3}) - 1" />. Bestimme:

   - Amplitude
   - Periode
   - Phasenverschiebung
   - Vertikale Verschiebung

4. Wenn die Gezeitenhöhe durch <InlineMath math="h(t) = 2 \sin(\frac{\pi t}{6}) + 5 \text{ Meter}" /> modelliert wird, wobei <InlineMath math="t" /> in Stunden gemessen wird:

   - Was sind die maximalen und minimalen Wasserhöhen?
   - Was ist die Gezeitenperiode?

5. Bestimme die Gleichung einer trigonometrischen Funktion mit:

   - Amplitude <InlineMath math="3" />
   - Periode <InlineMath math="\pi" />
   - <InlineMath math="\frac{\pi}{4}" /> nach rechts verschoben
   - <InlineMath math="2 \text{ Einheiten}" /> **nach oben** verschoben

### Winkelumrechnung

Rechne die Einheiten mit dem passenden **Umrechnungsfaktor** um.

- <InlineMath math="120^\circ\cdot\frac{\pi\,\mathrm{rad}}{180^\circ}=\frac{2\pi}{3}\,\mathrm{rad}" /> Bogenmaß
- <InlineMath math="\frac{5\pi}{6}\,\mathrm{rad}\cdot\frac{180^\circ}{\pi\,\mathrm{rad}}=150^\circ" />

Eine volle Umdrehung entspricht dreihundertsechzig Grad oder zwei Pi Radiant. Ihr Verhältnis ist eins. Der Umrechnungsfaktor ändert deshalb die Einheit, aber nicht die Größe des Winkels.

### Amplitude und Periode

Verwende den **Betrag des Koeffizienten** für die Amplitude. Das Minuszeichen spiegelt die Kosinuskurve, ohne ihre Amplitude negativ zu machen.

- <InlineMath math="y = 3 \sin(2x)" />: Amplitude <InlineMath math="3" />, Periode <InlineMath math="\frac{2\pi}{2} = \pi" />
- <InlineMath math="y = -2 \cos(\frac{x}{3})" />: Amplitude <InlineMath math="2" />, Periode <InlineMath math="\frac{2\pi}{1/3} = 6\pi" />

Bei der zweiten Kurve beträgt der Eingabekoeffizient ein Drittel. Das Kosinusargument wächst erst bei einer Eingabezunahme um sechs Pi um zwei Pi. Die Periode ist deshalb dreimal so lang wie beim Grundkosinus.

<BlockMath math="\frac{T}{3}=2\pi \quad\Rightarrow\quad T=6\pi" />

### Verschobene Kurve Skizzieren

Die Schreibweise <InlineMath math="x+\pi/3=x-(-\pi/3)" /> zeigt die **Verschiebung nach links**. Diese fünf Punkte bestimmen eine vollständige Periode:

- Amplitude: <InlineMath math="2" />
- Periode: <InlineMath math="2\pi" />
- Phasenverschiebung: <InlineMath math="\frac{\pi}{3}" /> nach links
- Vertikale Verschiebung: <InlineMath math="1 \text{ Einheit}" /> nach unten

| <InlineMath math="x" /> | <InlineMath math="-\pi/3" /> | <InlineMath math="\pi/6" /> | <InlineMath math="2\pi/3" /> | <InlineMath math="7\pi/6" /> | <InlineMath math="5\pi/3" /> |
| --- | --- | --- | --- | --- | --- |
| <InlineMath math="y" /> | <InlineMath math="-1" /> | <InlineMath math="1" /> | <InlineMath math="-1" /> | <InlineMath math="-3" /> | <InlineMath math="-1" /> |

<LineEquation
  title="Eine Periode Skizzieren"
  description="Die markierten Punkte zeigen Mittellinie, Hochpunkt und Tiefpunkt nach den Verschiebungen."
  data={[
    { points: Array.from({length: 161}, (_, i) => { const x = -Math.PI / 3 + i * 2 * Math.PI / 160; return {x, y: 2 * Math.sin(x + Math.PI / 3) - 1, z: 0}; }), smooth: false, showPoints: false, color: "#9333ea" },
    ...[{x: -Math.PI/3,y:-1,z:0},{x:Math.PI/6,y:1,z:0},{x:2*Math.PI/3,y:-1,z:0},{x:7*Math.PI/6,y:-3,z:0},{x:5*Math.PI/3,y:-1,z:0}].map((point) => ({points:[point],showPoints:true,color:"#ea580c"}))
  ]}
  showZAxis={false}
  cameraPosition={[2,-1,11]}
  cameraTarget={[2,-1,0]}
/>

### Gezeitenmodell

Der Wasserstand schwankt um eine **Mittellinie** von fünf Metern mit einer maximalen Abweichung von zwei Metern.

- Maximale Höhe: <InlineMath math="5 + 2 = 7 \text{ Meter}" />
- Minimale Höhe: <InlineMath math="5 - 2 = 3 \text{ Meter}" />
- Periode: <InlineMath math="\frac{2\pi}{\pi/6} = 12 \text{ Stunden}" />

Der Sinus liegt zwischen minus eins und eins. Das Modell liefert deshalb immer Höhen zwischen drei und sieben Metern. Während einer Periode wächst das Sinusargument um zwei Pi.

<BlockMath math="\frac{\pi}{6}T=2\pi \quad\Rightarrow\quad T=12" />

### Gleichung Aufstellen

Bestimme den Frequenzkoeffizienten aus der **gegebenen Periode** und setze die waagerechte Verschiebung in die Klammer ein.

<BlockMath math="2\pi/B=\pi\;\Rightarrow\;B=2" />

<BlockMath math="y = 3 \sin(2(x - \frac{\pi}{4})) + 2" />

Durch Ausmultiplizieren des Arguments ergibt sich die gleichwertige Form:

<BlockMath math="y = 3 \sin(2x - \frac{\pi}{2}) + 2" />
