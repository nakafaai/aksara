
export const metadata = {
  title: "Logarithmic Function Concept",
    description: "Logarithmic functions are inverses of exponential functions. Graphs, properties, and worked problems show how the two are related.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2025-05-18",
  dateModified: "2026-09-01",
  subject: "Functions and Their Modeling",
};

## What Does a Logarithmic Function Do

A logarithm answers a specific question: which exponent produces a given value? In that sense, logarithmic and exponential functions undo each other.

Start with this exponential equation:

<BlockMath math="2^3 = 8" />

The exponent that turns <InlineMath math="2" /> into <InlineMath math="8" /> is <InlineMath math="3" />. Logarithmic notation records the same relationship from the opposite direction:

<BlockMath math="\log_2 8 = 3" />

In general, exponential and logarithmic notation are related by:

<BlockMath math="y = b^x \quad \Leftrightarrow \quad x = \log_b y" />

## Definition and Types of Logarithms

A logarithmic function with base <InlineMath math="b" /> (where <InlineMath math="b > 0" /> and <InlineMath math="b \neq 1" />) is expressed as:

<BlockMath math="f(x) = \log_b x \quad \text{for every } x > 0" />

Three bases appear especially often:

1. **Common Logarithm** (base <InlineMath math="10" />): <InlineMath math="f(x) = \log x" />

   Example: <InlineMath math="\log 100 = 2" /> because <InlineMath math="10^2 = 100" />

2. **Natural Logarithm** (base <InlineMath math="e \approx 2.7183" />): <InlineMath math="f(x) = \ln x" />

   Example: <InlineMath math="\ln e = 1" /> because <InlineMath math="e^1 = e" />

3. **Binary Logarithm** (base <InlineMath math="2" />): <InlineMath math="f(x) = \log_2 x" />

   Example: <InlineMath math="\log_2 8 = 3" /> because <InlineMath math="2^3 = 8" />

## Logarithmic Function Graph

<LineEquation
  title="Comparison of Exponential and Logarithmic Functions"
  description={
    <>
      Graph <InlineMath math="y = \log_2 x" /> is the reflection of{" "}
      <InlineMath math="y = 2^x" /> across the line <InlineMath math="y = x" />.
    </>
  }
  data={[
    {
      points: Array.from({ length: 100 }, (_, i) => {
        const x = (i / 99) * 4 - 1;
        return { x, y: Math.pow(2, x), z: 0 };
      }),
      color: "#0284c7",
      labels: [{ text: <InlineMath math="y=2^x" />, at: 80, offset: [1.5, 1, 0] }],
      showPoints: false,
    },
    {
      points: Array.from({ length: 100 }, (_, i) => {
        const x = (i / 99) * 8 + 0.1;
        return { x, y: Math.log2(x), z: 0 };
      }),
      color: "#e11d48",
      labels: [{ text: <InlineMath math="y=\log_{2} x" />, at: 80, offset: [0.5, -1, 0] }],
      showPoints: false,
    },
    {
      points: Array.from({ length: 50 }, (_, i) => {
        const x = (i / 49) * 4 - 1;
        return { x, y: x, z: 0 };
      }),
      smooth: false,
      color: "#9333ea",
      labels: [{ text: <InlineMath math="y=x" />, at: 40, offset: [1, 1, 0] }],
      showPoints: false,
    },
  ]}
  cameraPosition={[0, 0, 15]}
  showZAxis={false}
/>

**Graph characteristics** <InlineMath math="f(x) = \log_b x" /> with <InlineMath math="b > 1" />:

- Domain: <InlineMath math="x > 0" /> (positive numbers only)
- Range: All real numbers
- <InlineMath math="x" />-intercept: <InlineMath math="(1, 0)" />
- Vertical asymptote: <InlineMath math="y" />-axis (<InlineMath math="x = 0" />)
- Increasing function for <InlineMath math="b > 1" />

## Properties of Logarithms

### Basic Properties

<MathContainer>
  <BlockMath math="\log_b 1 = 0" />
  <BlockMath math="\log_b b = 1" />
  <BlockMath math="\log_b b^n = n" />
  <BlockMath math="b^{\log_b x} = x" />
</MathContainer>

### Operational Properties

<MathContainer>
  <BlockMath math="\log_b (xy) = \log_b x + \log_b y" />
  <BlockMath math="\log_b \left(\frac{x}{y}\right) = \log_b x - \log_b y" />
  <BlockMath math="\log_b x^n = n \cdot \log_b x" />
  <BlockMath math="\log_b x = \frac{\log_a x}{\log_a b} \quad \text{(change of base)}" />
</MathContainer>

## Exponential Model from Two Data Points

Indonesia's Ministry of Health, or Kemenkes, reported two confirmed COVID-19 cases on 2 March 2020. [Kemenkes](https://kemkes.go.id/id/positif-covid-19-usai-kontak-dengan-wn-jepang)

On 30 April 2020, the ministry reported 10,118 cumulative confirmed cases. [Kemenkes](https://kemkes.go.id/id/pasien-positif-covid-19-capai-10-ribu)

The dates are <InlineMath math="59 \text{ elapsed days}" /> apart.

To practise logarithms, we can fit a simple exponential curve through those two reported values. Let <InlineMath math="t" /> be the number of elapsed days after 2 March and <InlineMath math="P(t)" /> the fitted case count. Since <InlineMath math="P(0)=2" /> and <InlineMath math="P(59)=10{,}118" />, the model is:

<ContentStack>

  <BlockMath math="P(t) = 2e^{\frac{1}{59}\ln(5059)t}" />

  <LineEquation
    title="Two-Point Exponential Fit"
    description="A fitted curve through the two reported case counts, followed by a short extrapolation"
    data={[
      {
        points: Array.from({ length: 72 }, (_, i) => {
          const t = i;
          const P = 2 * Math.exp((1 / 59) * Math.log(5059) * t);
          return { x: t, y: P, z: 0 };
        }),
        color: "#dc2626",
        labels: [{ text: <InlineMath math="P(t)" />, at: 3, offset: [2, -0.5, 0] }],
        showPoints: false,
      },
    ]}
    cameraPosition={[15, 10, 15]}
    showZAxis={false}
  />
</ContentStack>

If the same fitted rate were extended, logarithms would place the <InlineMath math="50{,}000" /> threshold at:

<MathContainer>
  <BlockMath math="50000 = 2e^{\frac{1}{59}\ln(5059)t}" />
  <BlockMath math="t = \frac{59 \cdot \ln(25000)}{\ln(5059)} \approx 70.1 \text{ days}" />
</MathContainer>

This calculation illustrates how the logarithmic model works. Real confirmed case counts also depend on testing, reporting, behaviour, and public-health measures. A fixed exponential rate is useful only while its assumptions still fit the situation.

## Exercises

1. Determine the value of:

   - <InlineMath math="\log_3 27" />
   - <InlineMath math="\log_5 \frac{1}{125}" />
   - <InlineMath math="\ln e^3" />

2. If <InlineMath math="\log_2 x = 4" />, determine the value of <InlineMath math="x" />.

3. Simplify: <InlineMath math="\log_2 8 + \log_2 4 - \log_2 2" />

4. An investment grows according to the formula <InlineMath math="A = 1000 \cdot 2^t" /> (in million rupiah). How many years are needed for the investment to become <InlineMath math="8 \text{ billion rupiah}" />?

### Worked Solutions

1. Read each logarithm as an exponent question. The required powers are:

   <MathContainer>
     <BlockMath math="3^3=27\quad\Longrightarrow\quad\log_3 27=3" />
     <BlockMath math="5^{-3}=\frac{1}{125}\quad\Longrightarrow\quad\log_5\frac{1}{125}=-3" />
     <BlockMath math="e^3=e^3\quad\Longrightarrow\quad\ln e^3=3" />
   </MathContainer>

2. Rewrite the logarithmic equation in exponential form:

   <BlockMath math="\log_2 x=4\quad\Longleftrightarrow\quad x=2^4=16" />

   Substituting <InlineMath math="x=16" /> gives <InlineMath math="\log_2 16=4" />, so the result checks out.

3. Evaluate the three powers of <InlineMath math="2" /> before combining them:

   <BlockMath math="\log_2 8+\log_2 4-\log_2 2=3+2-1=4" />

4. The formula measures <InlineMath math="A" /> in millions of rupiah, so <InlineMath math="8 \text{ billion rupiah}=8000 \text{ million rupiah}" />. Substitute the target and isolate the exponential term:

   <MathContainer>
     <BlockMath math="8000=1000\cdot2^t" />
     <BlockMath math="8=2^t" />
     <BlockMath math="2^3=2^t\quad\Longrightarrow\quad t=3" />
   </MathContainer>

   The investment reaches the target after <InlineMath math="3 \text{ years}" />.
