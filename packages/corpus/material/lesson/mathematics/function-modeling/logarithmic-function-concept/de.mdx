
export const metadata = {
  title: "Grundidee logarithmischer Funktionen",
  description: "Logarithmen als Umkehrung von Exponentialfunktionen verstehen: mit Graphen, Rechenregeln, nachvollziehbaren Beispielen und Übungen.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2026-08-22",
  dateModified: "2026-09-12",
  subject: "Funktionen und ihre Modellierung",
};

## Den gesuchten Exponenten als Logarithmus schreiben

Ein Logarithmus beantwortet eine konkrete Frage: Welcher Exponent erzeugt einen bestimmten Wert? In diesem Sinn machen logarithmische und exponentielle Funktionen einander rückgängig.

Ausgangspunkt ist diese Exponentialgleichung:

<BlockMath math="2^3 = 8" />

Der Exponent, der <InlineMath math="2" /> in <InlineMath math="8" /> verwandelt, ist <InlineMath math="3" />. Die logarithmische Schreibweise hält dieselbe Beziehung aus der umgekehrten Richtung fest:

<BlockMath math="\log_2 8 = 3" />

Allgemein hängen exponentielle und logarithmische Schreibweise so zusammen:

<BlockMath math="y = b^x \quad \Leftrightarrow \quad x = \log_b y" />

## Definition und Arten von Logarithmen

Eine logarithmische Funktion zur Basis <InlineMath math="b" /> mit <InlineMath math="b > 0" /> und <InlineMath math="b \neq 1" /> hat die Form:

<BlockMath math="f(x) = \log_b x \quad \text{für jeden} x &gt; 0" />

Drei Basen kommen besonders häufig vor:

1. **Zehnerlogarithmus** (Basis <InlineMath math="10" />): <InlineMath math="f(x) = \log x" />

   Beispiel: <InlineMath math="\log 100 = 2" /> weil <InlineMath math="10^2 = 100" />

2. **Natürlicher Logarithmus** (Basis <InlineMath math="e \approx 2{,}7183" />): <InlineMath math="f(x) = \ln x" />

   Beispiel: <InlineMath math="\ln e = 1" /> weil <InlineMath math="e^1 = e" />

3. **Binärer Logarithmus** (Basis <InlineMath math="2" />): <InlineMath math="f(x) = \log_2 x" />

   Beispiel: <InlineMath math="\log_2 8 = 3" /> weil <InlineMath math="2^3 = 8" />

## Graph einer logarithmischen Funktion

<LineEquation
  title="Vergleich von Exponential- und Logarithmusfunktionen"
  description={
    <>
      Der Graph von <InlineMath math="y = \log_2 x" /> ist die Spiegelung des
      Graphen von <InlineMath math="y = 2^x" /> an der Geraden{" "}
      <InlineMath math="y = x" />.
    </>
  }
  data={[
    {
      smooth: false,
      points: (() => {
        const parameters = Array.from(
          { length: 881 },
          (_, i) => -16 + (i / 880) * 22
        );
        return parameters
          .sort((a, b) => a - b)
          .map((t) => {
            const x = t;
            return { x, y: Math.pow(2, x), z: 0 };
          });
      })(),
      color: "#0284c7",
      labels: [{ text: <InlineMath math="y=2^x" />, at: 720, offset: [-0.7, 3, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: (() => {
        const parameters = Array.from(
          { length: 881 },
          (_, i) => -16 + (i / 880) * 22
        );
        return parameters
          .sort((a, b) => a - b)
          .map((t) => {
            const x = Math.pow(2, t);
            return { x, y: t, z: 0 };
          });
      })(),
      color: "#e11d48",
      labels: [{ text: <InlineMath math="y=\log_{2} x" />, at: 720, offset: [1.2, -1.2, 0] }],
      showPoints: false,
    },
    {
      points: [
      { x: -160, y: -160, z: 0 },
      { x: -3, y: -3, z: 0 },
      { x: 8, y: 8, z: 0 },
      { x: 160, y: 160, z: 0 },
      ],
      smooth: false,
      color: "#9333ea",
      labels: [{ text: <InlineMath math="y=x" />, at: 2, offset: [-0.8, -2.5, 0] }],
      showPoints: false,
    },
  ]}
  cameraPosition={[2.5, 2.5, 14]}
  cameraTarget={[2.5, 2.5, 0]}
  showZAxis
/>

**Eigenschaften des Graphen** von <InlineMath math="f(x) = \log_b x" /> für <InlineMath math="b > 1" />:

- Definitionsbereich: <InlineMath math="x > 0" />
- Wertebereich: alle reellen Zahlen
- Schnittpunkt mit der <InlineMath math="x" />-Achse: <InlineMath math="(1, 0)" />
- Senkrechte Asymptote: die <InlineMath math="y" />-Achse mit <InlineMath math="x = 0" />
- Die Funktion ist für <InlineMath math="b > 1" /> streng monoton steigend

## Eigenschaften von Logarithmen

### Grundlegende Eigenschaften

<MathContainer>
  <BlockMath math="\log_b 1 = 0" />
  <BlockMath math="\log_b b = 1" />
  <BlockMath math="\log_b b^n = n" />
  <BlockMath math="b^{\log_b x} = x" />
</MathContainer>

### Rechenregeln

<MathContainer>
  <BlockMath math="\log_b (xy) = \log_b x + \log_b y" />
  <BlockMath math="\log_b \left(\frac{x}{y}\right) = \log_b x - \log_b y" />
  <BlockMath math="\log_b x^n = n \cdot \log_b x" />
  <BlockMath math="\log_b x = \frac{\log_a x}{\log_a b} \quad \text{(Basiswechsel)}" />
</MathContainer>

## Exponentielles Modell aus zwei Datenpunkten

Das [indonesische Gesundheitsministerium](https://kemkes.go.id/id/positif-covid-19-usai-kontak-dengan-wn-jepang), kurz Kemenkes, meldete am 2. März 2020 zwei bestätigte COVID-19-Fälle.

Am 30. April 2020 [meldete das Ministerium](https://kemkes.go.id/id/pasien-positif-covid-19-capai-10-ribu) insgesamt 10.118 bestätigte Fälle.

Zwischen beiden Meldungen liegen <InlineMath math="59 \text{ vergangene Tage}" />.

Um Logarithmen anzuwenden, passen wir eine einfache Exponentialkurve an diese beiden gemeldeten Werte an. Dabei bezeichnet <InlineMath math="t" /> die seit dem 2. März vergangenen Tage und <InlineMath math="P(t)" /> die Fallzahl auf der Kurve. Aus <InlineMath math="P(0)=2" /> und <InlineMath math="P(59)=10{.}118" /> folgt:

<ContentStack>

  <BlockMath math="P(t) = 2e^{\frac{1}{59}\ln(5059)t}" />

  <LineEquation
    title="Exponentialfunktion durch zwei Datenpunkte"
    description={
      <>
        Eine Einheit auf der waagerechten Achse <InlineMath math="X=t/10" /> entspricht{" "}
        <InlineMath math="10" /> Tagen. Eine Einheit auf der senkrechten Achse{" "}
        <InlineMath math="Y=P(t)/10000" /> entspricht <InlineMath math="10{.}000" /> Fällen.
        Die beiden orangefarbenen Punkte zeigen gemeldete Werte. Blau zeigt die Extrapolation von{" "}
        <InlineMath math="t=59" /> bis <InlineMath math="t=71" />.
      </>
    }
    data={[
      {
        smooth: false,
        points: Array.from({ length: 237 }, (_, i) => {
          const t = i / 4;
          const P = 2 * Math.pow(5059, t / 59);
          return { x: t / 10, y: P / 10000, z: 0 };
        }),
        color: "#ea580c",
        labels: [
          { text: <InlineMath math="P(0)=2" />, at: 0, offset: [1.4, 0.65, 0] },
          { text: <InlineMath math="P(59)=10{.}118" />, at: 236, offset: [-1.7, 0.85, 0] },
        ],
        pointIndices: [0, 236],
        showPoints: true,
      },
      {
        smooth: false,
        points: Array.from({ length: 49 }, (_, i) => {
          const t = 59 + i / 4;
          const P = 2 * Math.pow(5059, t / 59);
          return { x: t / 10, y: P / 10000, z: 0 };
        }),
        color: "#0284c7",
        showPoints: false,
      },
    ]}
    cameraPosition={[3.3, 2.5, 10]}
    cameraTarget={[3.3, 2.5, 0]}
    showZAxis
  />
</ContentStack>

Wird die angepasste Rate fortgeschrieben, erreicht die Kurve den Schwellenwert <InlineMath math="50{.}000" /> laut logarithmischer Rechnung bei:

<MathContainer>
  <BlockMath math="50000 = 2e^{\frac{1}{59}\ln(5059)t}" />
  <BlockMath math="t = \frac{59 \cdot \ln(25000)}{\ln(5059)} \approx 70{,}1 \text{Tage}" />
</MathContainer>

Das ist eine mathematische Illustration, keine epidemiologische Prognose. Bestätigte Fallzahlen hängen auch von Tests, Meldesystemen, Verhalten und gesundheitspolitischen Maßnahmen ab. Eine feste exponentielle Rate darf deshalb nicht unbegrenzt fortgeschrieben werden.

## Übungen

1. Bestimme den Wert von:

   - <InlineMath math="\log_3 27" />
   - <InlineMath math="\log_5 \frac{1}{125}" />
   - <InlineMath math="\ln e^3" />

2. Wenn <InlineMath math="\log_2 x = 4" />, bestimme den Wert von <InlineMath math="x" />.

3. Vereinfache: <InlineMath math="\log_2 8 + \log_2 4 - \log_2 2" />

4. Eine Investition wächst nach der Formel <InlineMath math="A = 1000 \cdot 2^t" /> (in Millionen Rupiah). Wie viele Jahre sind nötig, bis die Investition <InlineMath math="8 \text{Milliarden Rupiah}" /> beträgt?

### Ausführliche Lösungen

1. Jeder Logarithmus ist als Frage nach einem Exponenten zu lesen. Die gesuchten Potenzen sind:

   <MathContainer>
     <BlockMath math="3^3=27\quad\Longrightarrow\quad\log_3 27=3" />
     <BlockMath math="5^{-3}=\frac{1}{125}\quad\Longrightarrow\quad\log_5\frac{1}{125}=-3" />
     <BlockMath math="e^3=e^3\quad\Longrightarrow\quad\ln e^3=3" />
   </MathContainer>

2. Zuerst wird die logarithmische Gleichung in die exponentielle Form umgeschrieben:

   <BlockMath math="\log_2 x=4\quad\Longleftrightarrow\quad x=2^4=16" />

   Mit <InlineMath math="x=16" /> ergibt die Probe <InlineMath math="\log_2 16=4" />. Das Ergebnis stimmt also.

3. Zunächst werden die drei Potenzen zur Basis <InlineMath math="2" /> bestimmt und anschließend verrechnet:

   <BlockMath math="\begin{aligned} &amp;\log_2 8+\log_2 4-\log_2 2 \\ &amp;=3+2-1 \\ &amp;=4 \end{aligned}" />

4. In der Formel wird <InlineMath math="A" /> in Millionen Rupiah angegeben. Daher gilt <InlineMath math="8 \text{ Milliarden Rupiah}=8000 \text{ Millionen Rupiah}" />. Nun wird der Zielwert eingesetzt und der Exponentialterm freigestellt:

   <MathContainer>
     <BlockMath math="8000=1000\cdot2^t" />
     <BlockMath math="8=2^t" />
     <BlockMath math="2^3=2^t\quad\Longrightarrow\quad t=3" />
   </MathContainer>

   Die Investition erreicht den Zielwert nach <InlineMath math="3 \text{ Jahren}" />.
