export const metadata = {
  title: "Horizontale Verschiebung",
  description: "Verschiebe Funktionsgraphen nach links oder rechts, leite die Vorzeichenregel her und verfolge Definitions- und Wertebereich.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2026-08-22",
  dateModified: "2026-09-19",
  subject: "Funktionstransformation",
};

## Den Graphen nach links oder rechts verschieben

Eine horizontale Verschiebung bewegt den Graphen einer Funktion entlang der <InlineMath math="x" />-Achse nach links oder rechts, ohne seine Form zu verändern. Wie bei einem Gegenstand, der über einen Tisch geschoben wird, <Highlight>ändert sich nur die Position</Highlight>.

Aus der Funktion <InlineMath math="f(x)" /> entsteht die verschobene Funktion <InlineMath math="g(x) = f(x - h)" />. Die Zahl <InlineMath math="h" /> gibt Richtung und Länge der Verschiebung an.

### Die Vorzeichenregel lesen

Sieh dir an, wo <InlineMath math="h" /> in der folgenden Gleichung steht. Die Zahl steht in der Klammer zusammen mit <InlineMath math="x" />, denn sie verändert die Eingabe, bevor die Funktion arbeitet.

<BlockMath math="g(x) = f(x - h)" />

Für die **gleiche Ausgabe** wie bei der alten Eingabe <InlineMath math="a" /> muss die neue Eingabe <InlineMath math="x-h=a" /> erfüllen. Also gilt <InlineMath math="x=a+h" />. Das erklärt die Richtung ohne auswendig gelernte Vorzeichenregeln.

<BlockMath math="g(a+h)=f((a+h)-h)=f(a)" />

Weil <InlineMath math="h" /> in der Klammer steht, wirkt sein Vorzeichen <Highlight>umgekehrt zur Leserichtung</Highlight>. Ziehst du eine positive Zahl von <InlineMath math="x" /> ab, verschiebt sich der Graph nach rechts. Addierst du eine positive Zahl, verschiebt er sich nach links.

| Wert von <InlineMath math="h" /> | Verschiebung des Graphen | Beispiel am Scheitelpunkt |
| --- | --- | --- |
| <InlineMath math="h > 0" /> | Um <InlineMath math="h \text{ Einheiten}" /> nach rechts | <InlineMath math="(0,0)" /> wird zu <InlineMath math="(h,0)" /> |
| <InlineMath math="h < 0" /> | Um <InlineMath math="\lvert h \rvert \text{ Einheiten}" /> nach links | <InlineMath math="(0,0)" /> wird zu <InlineMath math="(h,0)" /> |
| <InlineMath math="h = 0" /> | Der Graph verschiebt sich nicht | <InlineMath math="(0,0)" /> bleibt an seinem Platz |

Alle drei Zeilen nutzen den Scheitelpunkt <InlineMath math="(0,0)" /> als Bezugspunkt, damit sich die Richtung leicht vergleichen lässt. Bei einer Funktion mit einem anderen Bezugspunkt wandert jeder Punkt um dieselbe Strecke in dieselbe Richtung.

## Die Regel am Parabelgraphen prüfen

Stell dir drei Parabeln in derselben Ebene vor. Sie haben genau dieselbe Form, ihre Scheitelpunkte liegen aber an unterschiedlichen horizontalen Positionen.

<LineEquation
  title={<>Horizontale Verschiebung der quadratischen Funktion <InlineMath math="f(x) = x^2" /></>}
  description="Die Parabelform bleibt erhalten, während sich der Scheitelpunkt horizontal verschiebt."
  cameraPosition={[0.5, 3, 16]}
  cameraTarget={[0.5, 3, 0]}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 41 }, (_, i) => {
        const x = (i - 20) * 0.25;
        return { x, y: x * x, z: 0 };
      }),
      color: "#9333ea",
      labels: [{ text: <InlineMath math="f(x)=x^2" />, offset: [0.5, 1, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 193 }, (_, pointIndex) => {
        const i = pointIndex / 4 + 0;
        const x = (i - 20) * 0.25;
        return { x, y: (x - 3) * (x - 3), z: 0 };
      }),
      color: "#ea580c",
      labels: [{ text: <InlineMath math="g(x)=(x-3)^2" />, offset: [0.5, 1, 0], at: 80 }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 173 }, (_, pointIndex) => {
        const i = pointIndex / 4 - 3;
        const x = (i - 20) * 0.25;
        return { x, y: (x + 2) * (x + 2), z: 0 };
      }),
      color: "#0d9488",
      labels: [{ text: <InlineMath math="h(x)=(x+2)^2" />, offset: [0.5, 1, 0], at: 92 }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
/>

Die Scheitelpunkte liegen auf **derselben Höhe**. Vergleiche deshalb ihre waagerechten Koordinaten, um die Verschiebung abzulesen.

| Funktion | Scheitelpunkt | Verschiebung |
| --- | --- | --- |
| <InlineMath math="x^2" /> | <InlineMath math="(0,0)" /> | <InlineMath math="0" /> |
| <InlineMath math="(x-3)^2" /> | <InlineMath math="(3,0)" /> | <InlineMath math="+3" /> |
| <InlineMath math="(x+2)^2" /> | <InlineMath math="(-2,0)" /> | <InlineMath math="-2" /> |

## Die Regel am Geradengraphen prüfen

**Dieselbe Regel** gilt für die lineare Funktion <InlineMath math="f(x) = 2x + 1" />. Bei einer Geraden ändert eine horizontale Verschiebung die Steigung nicht. Das Vorzeichen lässt sich deshalb noch leichter prüfen als bei einer Parabel.

<LineEquation
  title={<>Horizontale Verschiebung der linearen Funktion <InlineMath math="f(x) = 2x + 1" /></>}
  description="Alle drei Geraden haben dieselbe Steigung, liegen aber an unterschiedlichen Positionen."
  cameraPosition={[1, 1, 16]}
  cameraTarget={[1, 1, 0]}
  data={[
    {
      points: Array.from({ length: 39 }, (_, pointIndex) => {
        const i = pointIndex - 18;
        const x = (i - 10) * 0.5;
        return { x, y: 2 * x + 1, z: 0 };
      }),
      smooth: false,
      color: "#7c3aed",
      labels: [{ text: <InlineMath math="f(x)=2x+1" />, offset: [1, 0.5, 0], at: 28 }],
      showPoints: false,
    },
    {
      points: Array.from({ length: 34 }, (_, pointIndex) => {
        const i = pointIndex - 5;
        const x = (i - 10) * 0.5;
        return { x, y: 2 * (x - 4) + 1, z: 0 };
      }),
      smooth: false,
      color: "#d97706",
      labels: [{ text: <InlineMath math="g(x)=2(x-4)+1" />, offset: [1, 0.5, 0], at: 15 }],
      showPoints: false,
    },
    {
      points: Array.from({ length: 62 }, (_, pointIndex) => {
        const i = pointIndex - 41;
        const x = (i - 10) * 0.5;
        return { x, y: 2 * (x + 3) + 1, z: 0 };
      }),
      smooth: false,
      color: "#0891b2",
      labels: [{ text: <InlineMath math="h(x)=2(x+3)+1" />, offset: [-0.7, -2.3, 0], at: 51 }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
/>

Die violette Gerade ist die ursprüngliche Gerade mit der Steigung <InlineMath math="2" />. Die orange Gerade gehört zu <InlineMath math="g(x) = 2(x-4)+1" /> und ist um <InlineMath math="4 \text{ Einheiten}" /> nach rechts verschoben. Die blaue Gerade gehört zu <InlineMath math="h(x) = 2(x+3)+1" /> und ist um <InlineMath math="3 \text{ Einheiten}" /> nach links verschoben. Alle drei Geraden bleiben parallel, weil sich die Steigung nicht ändert.

In beiden Beispielen kommt die Richtung der Verschiebung immer aus dem Vorzeichen in der Klammer. Das ist das Gegenteil einer vertikalen Verschiebung, die ihre Konstante außerhalb der Funktion addiert.

## Was die horizontale Verschiebung mitnimmt

Wenn du die Richtung kennst, bleibt noch die Frage, welche Teile der Funktion mitwandern. Eine horizontale Verschiebung greift an der Eingabe an. Alles, was von der Lage von <InlineMath math="x" /> abhängt, bewegt sich also mit, während die Ausgabewerte gleich bleiben.

Betrachte einen Punkt <InlineMath math="(a, b)" /> auf dem Graphen von <InlineMath math="f(x)" />. Nach einer Verschiebung um <InlineMath math="h" /> liegt dieser Punkt als <InlineMath math="(a + h, b)" /> auf dem Graphen von <InlineMath math="f(x - h)" />. Der <InlineMath math="y" />-Wert bleibt gleich, nur die <InlineMath math="x" />-Koordinate wandert.

| Teil der Funktion | Vor der Verschiebung | Nach der Verschiebung um <InlineMath math="h" /> |
| --- | --- | --- |
| Form des Graphen | Die ursprüngliche Form | Dieselbe Form, nur horizontal verschoben |
| Punkt <InlineMath math="(a, b)" /> | Auf dem Graphen von <InlineMath math="f(x)" /> | Wandert zu <InlineMath math="(a + h, b)" /> |
| Definitionsbereich <InlineMath math="[c, d]" /> | Das ursprüngliche Intervall | Wird zu <InlineMath math="[c + h, d + h]" /> |
| Wertebereich | Die ursprünglichen Ausgabewerte | Dieselben Werte wie vorher |

Der Definitionsbereich wandert mit, weil er die erlaubten Werte von <InlineMath math="x" /> beschreibt. Der Wertebereich bleibt gleich, weil die Höhe des Graphen an jeder Stelle unverändert ist. Bei einer vertikalen Verschiebung tauschen die beiden Bereiche ihre Rollen.

Die **Ausgabehöhe bleibt gleich** am verschobenen Punkt, nicht bei unveränderter Eingabe. Beispielsweise gilt <InlineMath math="g(a+h)=f(a)" />, obwohl <InlineMath math="g(a)" /> im Allgemeinen von <InlineMath math="f(a)" /> abweicht.

## Die Regel am Exponentialgraphen prüfen

Bei der Exponentialkurve bleibt die **waagerechte Asymptote unverändert**. Den senkrechten Achsenabschnitt berechnest du bei der Eingabe null neu.

<LineEquation
  title={<>Horizontale Verschiebung der Exponentialfunktion <InlineMath math="f(x) = 2^x" /></>}
  description="Die Exponentialkurve behält ihre Eigenschaften nach der horizontalen Verschiebung bei."
  cameraPosition={[1, 3, 12]}
  cameraTarget={[1, 3, 0]}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 961 }, (_, pointIndex) => {
        const x = -4 + (pointIndex / 960) * 8;
        return { x, y: Math.pow(2, x), z: 0 };
      }),
      color: "#4f46e5",
      labels: [{ text: <InlineMath math="f(x)=2^x" />, offset: [0.5, 1, 0], at: 480 }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 969 }, (_, pointIndex) => {
        const x = -4 + (pointIndex / 968) * 8;
        return { x, y: Math.pow(2, x - 2), z: 0 };
      }),
      color: "#059669",
      labels: [{ text: <InlineMath math="g(x)=2^{x-2}" />, offset: [0.5, 1, 0], at: 480 }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 481 }, (_, pointIndex) => {
        const x = -4 + (pointIndex / 480) * 8;
        return { x, y: Math.pow(2, x + 1), z: 0 };
      }),
      color: "#e11d48",
      labels: [{ text: <InlineMath math="h(x)=2^{x+1}" />, offset: [0.5, 1, 0], at: 240 }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
/>

Die horizontale Asymptote bleibt bei <InlineMath math="y = 0" />, weil eine horizontale Verschiebung das Verhalten der Kurve für sehr große und sehr kleine <InlineMath math="x" /> nicht ändert. Die Funktion <InlineMath math="g(x) = 2^{x-2}" /> verschiebt sich um <InlineMath math="2 \text{ Einheiten}" /> nach rechts, während <InlineMath math="h(x) = 2^{x+1}" /> um <InlineMath math="1 \text{ Einheit}" /> nach links wandert.

Berechne den senkrechten Achsenabschnitt neu mit der **Eingabe null**. Der alte Punkt <InlineMath math="(0,f(0))" /> wandert nach <InlineMath math="(h,f(0))" /> und liegt im Allgemeinen nicht mehr auf der senkrechten Achse. Der neue Achsenabschnitt lautet <InlineMath math="(0,f(-h))" />, sofern <InlineMath math="-h" /> im Definitionsbereich liegt.

<BlockMath math="f(0)=1,\qquad g(0)=2^{-2}=\frac14,\qquad h(0)=2^1=2" />

## Horizontale und vertikale Verschiebung unterscheiden

Die horizontale und die [vertikale Verschiebung](/de/faecher/mathematik/funktionstransformationen/vertikale-verschiebung) bewegen beide einen Graphen, greifen aber an unterschiedlichen Stellen der Vorschrift an. Dieser Unterschied entscheidet, welche Koordinate sich ändert.

| Merkmal | Horizontale Verschiebung | Vertikale Verschiebung |
| --- | --- | --- |
| Vorschrift | <InlineMath math="g(x) = f(x - h)" /> | <InlineMath math="g(x) = f(x) + k" /> |
| Veränderter Teil | Die Funktionseingabe | Die Funktionsausgabe |
| Richtung der Verschiebung | Entlang der <InlineMath math="x" />-Koordinate | Entlang der <InlineMath math="y" />-Koordinate |
| Vorzeichen der Konstanten | Umgekehrt zur Leserichtung | Wie das Vorzeichen von <InlineMath math="k" /> |
| **Definitionsbereich** und Wertebereich | Der Definitionsbereich wandert, der Wertebereich bleibt | Der Definitionsbereich bleibt, der Wertebereich wandert |

Der wichtigste Unterschied ist die Lage der Konstanten. Eine Konstante in der Klammer verändert die Eingabe. Die Verschiebung läuft deshalb horizontal, und ihr Vorzeichen wirkt umgekehrt. Eine Konstante außerhalb der Klammer verändert die Ausgabe. Die Verschiebung läuft deshalb vertikal, und ihr Vorzeichen wirkt wie geschrieben.

Eine [horizontale Streckung und Stauchung](/de/faecher/mathematik/funktionstransformationen/horizontale-streckung-und-stauchung) verändert die Breite des Graphen, eine [vertikale Streckung und Stauchung](/de/faecher/mathematik/funktionstransformationen/vertikale-streckung-und-stauchung) den Abstand zur waagerechten Achse. Eine Verschiebung addiert dagegen einen festen Abstand. Bei [kombinierten Funktionstransformationen](/de/faecher/mathematik/funktionstransformationen/kombinierte-funktionstransformationen) kann Verschieben vor dem Skalieren eine andere Endlage ergeben als Verschieben danach.

## Übungen

Die drei Aufgaben nutzen die Vorzeichenregel, die du gerade gelernt hast. Rechne sie zuerst ohne die Lösungen und vergleiche danach deine Schritte mit den ausführlichen Rechenwegen.

1. Gegeben ist die Funktion <InlineMath math="f(x) = x^2 + 2x + 1" />. Bestimme die Gleichung nach einer horizontalen Verschiebung um <InlineMath math="3 \text{ Einheiten}" /> nach rechts.

2. Der Graph der Funktion <InlineMath math="g(x) = \sqrt{x}" /> wird horizontal um <InlineMath math="4 \text{ Einheiten}" /> nach links verschoben. Bestimme die Gleichung der verschobenen Funktion und danach den **Definitionsbereich** nach der Verschiebung.

3. Die Funktion <InlineMath math="h(x) = 3^x" /> wird so verschoben, dass der Punkt <InlineMath math="(0, 1)" /> auf <InlineMath math="(2, 1)" /> abgebildet wird. Bestimme die Verschiebungskonstante und die Gleichung der verschobenen Funktion.

### Parabel Verschieben

Ersetze **jedes Vorkommen der Eingabe**:

Horizontale Verschiebung nach rechts um <InlineMath math="3 \text{ Einheiten}" />:

<BlockMath math="F(x) = f(x - 3) = (x - 3)^2 + 2(x - 3) + 1 = x^2 - 4x + 4" />

<LineEquation
  title={<>Funktion <InlineMath math="f(x) = x^2 + 2x + 1" /> und verschobener Graph</>}
  description={
    <>
      Ursprüngliche quadratische Funktion und Verschiebung um
      <InlineMath math="3 \text{ Einheiten}" /> nach rechts.
    </>
  }
  cameraPosition={[0.5, 3, 16]}
  cameraTarget={[0.5, 3, 0]}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 41 }, (_, i) => {
        const x = (i - 20) * 0.25;
        return { x, y: x * x + 2 * x + 1, z: 0 };
      }),
      color: "#9333ea",
      labels: [{ text: <InlineMath math="f(x)=x^2+2x+1" />, offset: [1, 1, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 41 }, (_, i) => {
        const x = (i - 20) * 0.25;
        return { x, y: x * x - 4 * x + 4, z: 0 };
      }),
      color: "#ea580c",
      labels: [{ text: <InlineMath math="F(x)=x^2-4x+4" />, offset: [1, 1, 0] }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
/>

### Definitionsbereich der Wurzelfunktion

Gleichung der verschobenen Funktion:

- Verschiebung nach links um <InlineMath math="4 \text{ Einheiten}" />: <InlineMath math="G(x) = g(x + 4) = \sqrt{x + 4}" />
- **Definitionsbereich** nach der Verschiebung: <InlineMath math="x + 4 \geq 0" />, also <InlineMath math="x \geq -4" /> oder <InlineMath math="[-4, \infty)" />

Visualisierung:

<LineEquation
  title={<>Funktion <InlineMath math="g(x) = \sqrt{x}" /> und verschobener Graph</>}
  description={
    <>
      Ursprüngliche Quadratwurzelfunktion und Verschiebung um
      <InlineMath math="4 \text{ Einheiten}" /> nach links.
    </>
  }
  cameraPosition={[3, 2, 14]}
  cameraTarget={[3, 2, 0]}
  data={[
    {
      smooth: false,
      points: (() => {
        const parameters = Array.from({ length: 161 }, (_, i) => i / 40);
        parameters.push(1.5811388300841898);
        return parameters
          .sort((a, b) => a - b)
          .map((t) => {
            const x = t * t;
            return { x, y: t, z: 0 };
          });
      })(),
      color: "#7c3aed",
      labels: [{ at: 64, text: <InlineMath math="g(x)=\sqrt{x}" />, offset: [1, 0.5, 0] }],
      showPoints: false,
    },
    {
      smooth: false,
      points: (() => {
        const parameters = Array.from({ length: 161 }, (_, i) => i / 40);
        parameters.push(1.5811388300841898);
        return parameters
          .sort((a, b) => a - b)
          .map((t) => {
            const x = t * t + -4;
            return { x, y: t, z: 0 };
          });
      })(),
      color: "#0d9488",
      labels: [{ text: <InlineMath math="G(x)=\sqrt{x+4}" />, offset: [1, 0.5, 0], at: 64 }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
/>

### Exponentialpunkt Verschieben

Punkt <InlineMath math="(0, 1)" /> auf <InlineMath math="h(x) = 3^x" /> wird zu <InlineMath math="(2, 1)" />, was eine **horizontale Verschiebung** um <InlineMath math="h = 2 \text{ Einheiten}" /> nach rechts bedeutet.
Die verschobene Funktion lautet <InlineMath math="H(x) = 3^{x-2}" />.

<LineEquation
  title={<>Funktion <InlineMath math="h(x) = 3^x" /> und verschobener Graph</>}
  description={
    <>
      Ursprüngliche Exponentialfunktion und Verschiebung um
      <InlineMath math="2 \text{ Einheiten}" /> nach rechts.
    </>
  }
  cameraPosition={[1, 2, 10]}
  cameraTarget={[1, 2, 0]}
  data={[
    {
      smooth: false,
      points: Array.from({ length: 285 }, (_, pointIndex) => {
        const x = -4 + (pointIndex / 284) * 8;
        return { x, y: Math.pow(3, x), z: 0 };
      }),
      color: "#4f46e5",
      labels: [{ text: <InlineMath math="h(x)=3^x" />, offset: [-1.3, 2, 0], at: 142 }],
      showPoints: false,
    },
    {
      smooth: false,
      points: Array.from({ length: 317 }, (_, pointIndex) => {
        const x = -4 + (pointIndex / 316) * 8;
        return { x, y: Math.pow(3, x - 2), z: 0 };
      }),
      color: "#059669",
      labels: [{ text: <InlineMath math="H(x)=3^{x-2}" />, offset: [3.2, 0.988889, 0], at: 142 }],
      showPoints: false,
    },
  ]}
  showZAxis={false}
/>
