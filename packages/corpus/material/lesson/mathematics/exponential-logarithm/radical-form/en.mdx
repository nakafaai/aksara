export const metadata = {
  title: "Radical Form",
  description:
    "Connect fractional exponents with roots, convert between both forms, and simplify radical expressions without using invalid shortcuts.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  date: "2025-04-04",
  subject: "Exponents and Logarithms",
};

## Relationship Between Exponents and Radicals

Fractional exponents and roots are two ways to describe the same operation. A decay model makes that connection concrete.

Suppose <InlineMath math="f(x)" /> is the amount of a medicine remaining in the body <InlineMath math="x" /> hours after a dose. The initial amount is <InlineMath math="50" /> units, and the amount halves every hour:

<BlockMath math="f(x)=50(0.5)^x" />

Thirty minutes is half an hour, so the correct input is:

<BlockMath math="x=\frac{30}{60}=\frac12" />

The amount remaining after thirty minutes is therefore:

<BlockMath math="f\left(\frac12\right)=50(0.5)^{\frac12}=50\sqrt{0.5}\approx35.36" />

The equality <InlineMath math="(0.5)^{\frac12}=\sqrt{0.5}" /> shows the bridge between exponent notation and radical notation. The model also makes its unit explicit, so <InlineMath math="x=30" /> would mean thirty hours, not thirty minutes.

## Definition of Radical Form

For <InlineMath math="a>0" />, <InlineMath math="m\in\mathbb{Z}" />, and <InlineMath math="n\in\mathbb{N}" />, the real-valued definition is:

<BlockMath math="a^{\frac{m}{n}}=\left(\sqrt[n]{a}\right)^m=\sqrt[n]{a^m}" />

This lets us move between fractional exponents and roots without changing the value. If <InlineMath math="n" /> is odd, negative real bases can also have real <InlineMath math="n" />th roots. If <InlineMath math="n" /> is even, a negative radicand has no real root.

## Simplifying Radical Forms

Simplify <InlineMath math="(2\sqrt{x})(3\sqrt[3]{x})" /> for <InlineMath math="x>0" />.

First write both roots as powers, combine the coefficients, and add the exponents:

<BlockMath
  math="\begin{align}
(2\sqrt{x})(3\sqrt[3]{x}) &= (2x^{\frac{1}{2}})(3x^{\frac{1}{3}}) \\
&= 2 \cdot 3 \cdot x^{\frac{1}{2} + \frac{1}{3}} \\
&= 6x^{\frac{3+2}{6}} \\
&= 6x^{\frac{5}{6}} \\
&= 6\sqrt[6]{x^5}
\end{align}"
/>

So the equivalent simplified forms are <InlineMath math="6x^{\frac56}" /> and <InlineMath math="6\sqrt[6]{x^5}" />.

## A Common Invalid Shortcut

A root does not distribute over addition. In general:

<BlockMath math="\sqrt{a+b}\neq\sqrt{a}+\sqrt{b}" />

One counterexample is enough to disprove the proposed identity. Take <InlineMath math="a=4" /> and <InlineMath math="b=9" />:

<BlockMath math="\sqrt{4+9}=\sqrt{13}\neq\sqrt4+\sqrt9=2+3=5" />

The mismatch is even clearer with perfect squares on both sides of the addition:

<MathContainer>
  <BlockMath math="\sqrt{16+9}=\sqrt{25}=5" />
  <BlockMath math="\sqrt{16}+\sqrt9=4+3=7" />
</MathContainer>

Because <InlineMath math="5\neq7" />, the two expressions cannot be interchangeable. Keep the sum inside the radical unless another valid algebraic step changes it.
