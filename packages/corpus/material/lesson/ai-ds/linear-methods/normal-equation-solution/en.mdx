export const metadata = {
  title: "Solving Normal Equations",
  description: "Solve linear least-squares problems with QR decomposition and the Moore-Penrose pseudoinverse, then compare their numerical behavior with normal equations.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2025-07-15",
  dateModified: "2026-08-22",
  subject: "Linear Methods of AI",
};

## What the System Solves

Given <InlineMath math="A \in \mathbb{R}^{m \times n}" /> and <InlineMath math="b \in \mathbb{R}^m" />, a linear least-squares problem asks for the parameter vector that makes the residual as small as possible:

<BlockMath math="\min_x \|Ax-b\|_2^2" />

Every minimizer satisfies the normal equation

<BlockMath math="A^T A\hat{x}=A^T b" />

This equation is always consistent. The minimizer is unique exactly when <InlineMath math="\operatorname{rank}(A)=n" />, which requires <InlineMath math="m\ge n" />. In that full-column-rank case,

<BlockMath math="\hat{x}=(A^T A)^{-1}A^T b" />

When <InlineMath math="A" /> is rank deficient, the fitted vector <InlineMath math="A\hat{x}" /> is still unique, but the parameter vector is not. If <InlineMath math="\hat{x}" /> is one solution, then every solution is in

<BlockMath math="\hat{x}+\ker(A)" />

### Why a Solution Always Exists

The column space of <InlineMath math="A" /> is a finite-dimensional closed subspace of <InlineMath math="\mathbb{R}^m" />. Therefore, <InlineMath math="b" /> has a unique orthogonal projection onto it. Any <InlineMath math="\hat{x}" /> satisfying <InlineMath math="A\hat{x}=\operatorname{proj}_{\operatorname{col}(A)}b" /> is a least-squares solution.

The normal equation expresses the same geometry because

<BlockMath math="A^T(A\hat{x}-b)=0" />

says that the residual <InlineMath math="A\hat{x}-b" /> is orthogonal to every column of <InlineMath math="A" />.

## Moore-Penrose Pseudoinverse

The Moore-Penrose pseudoinverse is defined for every real matrix, including rectangular and rank-deficient matrices. If

<BlockMath math="A=U\Sigma V^T" />

is a singular value decomposition, replace every nonzero singular value <InlineMath math="\sigma_i" /> by <InlineMath math="1/\sigma_i" /> and leave every zero singular value at zero. This gives

<MathContainer>
<BlockMath math="\Sigma^{\dagger}_{ii}=\begin{cases}1/\sigma_i,&\sigma_i>0,\\0,&\sigma_i=0,\end{cases}" />
<BlockMath math="A^{\dagger}=V\Sigma^{\dagger}U^T" />
<BlockMath math="x^{\dagger}=A^{\dagger}b" />
</MathContainer>

The vector <InlineMath math="x^{\dagger}" /> is the unique least-squares solution with minimum Euclidean norm. When <InlineMath math="A" /> has full column rank, the general definition simplifies to

<BlockMath math="A^{\dagger}=(A^T A)^{-1}A^T" />

The pseudoinverse is uniquely characterized by the four Penrose identities:

<MathContainer>
<BlockMath math="AA^{\dagger}A=A" />
<BlockMath math="A^{\dagger}AA^{\dagger}=A^{\dagger}" />
<BlockMath math="(AA^{\dagger})^T=AA^{\dagger}" />
<BlockMath math="(A^{\dagger}A)^T=A^{\dagger}A" />
</MathContainer>

## Direct Solution with QR Decomposition

For a full-column-rank matrix with <InlineMath math="m\ge n" />, use the thin QR decomposition

<BlockMath math="A=Q_1R_1,\qquad Q_1^TQ_1=I" />

The least-squares objective separates into

<BlockMath math="\|Ax-b\|_2^2=\|R_1x-Q_1^Tb\|_2^2+\|(I-Q_1Q_1^T)b\|_2^2" />

The second term does not depend on <InlineMath math="x" />. Therefore, the minimizer is obtained by solving the upper-triangular system

<BlockMath math="R_1\hat{x}=Q_1^Tb" />

This route solves the original least-squares problem directly. It does not form <InlineMath math="A^TA" /> and then solve the normal equation.

## Worked Polynomial-Fit Example

Fit <InlineMath math="y=at^2+bt+c" /> to seven observations. The design matrix and observation vector are

<MathContainer>
<BlockMath math="A=\begin{pmatrix}9&-3&1\\4&-2&1\\1&-1&1\\0&0&1\\1&1&1\\4&2&1\\9&3&1\end{pmatrix}" />
<BlockMath math="b=\begin{pmatrix}-2.2\\-4.2\\-4.2\\-1.8\\1.8\\8.2\\15.8\end{pmatrix}" />
</MathContainer>

Each row of <InlineMath math="A" /> is <InlineMath math="[t_i^2,t_i,1]" />. One valid thin QR decomposition is

<MathContainer>
<BlockMath math="Q_1=\begin{pmatrix}-0.64286&-0.56695&0.16496\\-0.28571&-0.37796&-0.24744\\-0.07143&-0.18898&-0.49487\\0&0&-0.57735\\-0.07143&0.18898&-0.49487\\-0.28571&0.37796&-0.24744\\-0.64286&0.56695&0.16496\end{pmatrix}" />
<BlockMath math="R_1=\begin{pmatrix}-14&0&-2\\0&5.29150&0\\0&0&-1.73205\end{pmatrix}" />
</MathContainer>

Signs of matching columns of <InlineMath math="Q_1" /> and rows of <InlineMath math="R_1" /> may change between valid QR implementations. The product <InlineMath math="Q_1R_1" /> and final fit do not change.

First compute

<BlockMath math="Q_1^Tb=\begin{pmatrix}-9.71429\\16.02569\\3.48060\end{pmatrix}" />

Then use back substitution:

<MathContainer>
<BlockMath math="-1.73205x_3=3.48060\quad\Longrightarrow\quad x_3=-2.00952" />
<BlockMath math="5.29150x_2=16.02569\quad\Longrightarrow\quad x_2=3.02857" />
<BlockMath math="-14x_1-2x_3=-9.71429\quad\Longrightarrow\quad x_1=0.98095" />
</MathContainer>

Thus,

<MathContainer>
<BlockMath math="\hat{x}=\begin{pmatrix}0.98095\\3.02857\\-2.00952\end{pmatrix}" />
<BlockMath math="\hat{y}(t)=0.98095t^2+3.02857t-2.00952" />
<BlockMath math="\|A\hat{x}-b\|_2\approx0.41861" />
</MathContainer>

<LineEquation
  title={<>Least-squares fit <InlineMath math="\hat{y}(t)=0.98095t^2+3.02857t-2.00952" /></>}
  description={<>The cyan curve is the fitted polynomial, while the orange points are the seven observations in <InlineMath math="b" />.</>}
  cameraPosition={[15, 10, 15]}
  data={(() => {
    // Observations encoded by A and b
    const originalData = [
      [-3, -2.2],
      [-2, -4.2],
      [-1, -4.2],
      [0, -1.8],
      [1, 1.8],
      [2, 8.2],
      [3, 15.8]
    ];

    // Coefficients obtained from the QR solve
    const a = 0.98095;
    const b = 3.02857;
    const c = -2.00952;

    return [
      {
        points: Array.from({ length: 50 }, (_, i) => {
          const t = -3 + (i * 6) / 49;
          const y = a * t * t + b * t + c;
          return { x: t, y: y, z: 0 };
        }),
        color: "#0891b2",
        smooth: true,
        showPoints: false
      },
      {
        points: originalData.map(([t, y]) => ({ x: t, y: y, z: 0 })),
        color: "#ea580c",
        smooth: false,
        showPoints: true
      }
    ];
  })()}
/>

## Choosing a Numerical Method

| Method | Approximate dense cost for <InlineMath math="m\ge n" /> | Main strength | Main limitation |
| --- | --- | --- | --- |
| Normal equations and Cholesky | form <InlineMath math="A^TA" /> in <InlineMath math="O(mn^2)" />, then factor in about <InlineMath math="\frac{1}{3}n^3" /> flops | fast when the problem is well conditioned and full rank | <InlineMath math="\kappa_2(A^TA)=\kappa_2(A)^2" /> |
| Householder QR | about <InlineMath math="2mn^2-\frac{2}{3}n^3" /> flops | avoids forming <InlineMath math="A^TA" /> and is usually more stable | requires a rank-aware variant when columns are dependent |
| SVD | more work than QR | exposes rank and gives the minimum-norm solution | highest cost of the three |

Use QR as the usual dense full-rank method. Use SVD when rank deficiency, severe ill-conditioning, or the minimum-norm solution matters. Form the normal matrix only when its speed advantage is worth the loss of numerical robustness, and never compute the inverse explicitly in production code.
