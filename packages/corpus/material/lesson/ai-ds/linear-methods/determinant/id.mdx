export const metadata = {
    title: "Definisi Determinan",
   description: "Jelaskan hubungan determinan dengan faktor volume berorientasi, turunkan sifat yang menentukan determinan, lalu gunakan determinan nol untuk memeriksa peringkat dan apakah matriks dapat dibalik.",
    authors: [{ name: "Nabil Akbarazzima Fatih" }],
    datePublished: "2025-07-10",
    dateModified: "2026-09-01",
    subject: "Metode Linear AI",
};

## Determinan

Determinan matriks persegi mengukur faktor perubahan volume berorientasi dari transformasi linearnya. Nilai mutlaknya memberi faktor volume, sedangkan tandanya menunjukkan apakah orientasi dipertahankan atau dibalik.

Setiap matriks persegi memiliki satu determinan. Determinan nol berarti hasil transformasi berada dalam himpunan berdimensi lebih rendah. Karena itu, transformasi tidak memiliki invers.

Determinan adalah fungsi khusus yang mengambil matriks persegi dan menghasilkan satu bilangan real:

<BlockMath math="\det : \mathbb{R}^{n \times n} \to \mathbb{R} : A \mapsto \det A" />

Tanda dan nilai determinan memberi tiga kemungkinan berikut:

| Determinan | Efek geometris | Dapat Dibalik |
|---|---|---|
| <InlineMath math="\det A>0" /> | Mengalikan volume dengan <InlineMath math="\lvert\det A\rvert" /> dan mempertahankan orientasi | Dapat dibalik |
| <InlineMath math="\det A<0" /> | Mengalikan volume dengan <InlineMath math="\lvert\det A\rvert" /> dan membalik orientasi | Dapat dibalik |
| <InlineMath math="\det A=0" /> | Mengubah volume menjadi nol dan menghasilkan himpunan berdimensi lebih rendah | Tidak dapat dibalik |

## Tiga Sifat Penentu Determinan

Hanya ada satu fungsi determinan yang memenuhi tiga sifat berikut.

### Sifat Linear pada Setiap Baris

Determinan bersifat **linear pada setiap baris ketika baris lain tetap**. Sifat ini disebut multilinearitas.

Ketika kita menjumlahkan dua vektor dalam satu baris:

<MathContainer>
<BlockMath math="\det \begin{pmatrix} a_1 \\ \vdots \\ a_i + a_i' \\ \vdots \\ a_n \end{pmatrix} = \det \begin{pmatrix} a_1 \\ \vdots \\ a_i \\ \vdots \\ a_n \end{pmatrix}" />
<BlockMath math="+ \det \begin{pmatrix} a_1 \\ \vdots \\ a_i' \\ \vdots \\ a_n \end{pmatrix}" />
</MathContainer>

Ketika kita mengalikan satu baris dengan skalar:

<MathContainer>
<BlockMath math="\det \begin{pmatrix} a_1 \\ \vdots \\ \lambda \cdot a_i \\ \vdots \\ a_n \end{pmatrix}" />
<BlockMath math="= \lambda \cdot \det \begin{pmatrix} a_1 \\ \vdots \\ a_i \\ \vdots \\ a_n \end{pmatrix}" />
</MathContainer>

### Sifat Berselang

Determinan bersifat **berselang-seling**: jika dua baris sama, nilainya nol.

<BlockMath math="\det \begin{pmatrix} \vdots \\ a \\ \vdots \\ a \\ \vdots \end{pmatrix} = 0" />

Secara ekuivalen, pertukaran dua baris membalik tanda determinan. Dua baris yang sama tidak berubah ketika ditukar, tetapi determinannya juga harus berganti tanda, sehingga satu-satunya nilai yang mungkin adalah nol.

### Sifat Normalisasi

Determinan **dinormalisasi** sehingga determinan matriks identitas selalu bernilai <InlineMath math="1" />:

<BlockMath math="\det I = 1" />

Di sini <InlineMath math="I" /> adalah matriks identitas dengan ukuran yang sesuai. Multilinearitas, sifat berselang-seling, dan normalisasi ini menentukan determinan secara tunggal.

## Operasi Matriks dan Determinan

Jika seluruh matriks dikalikan dengan skalar <InlineMath math="\lambda" />, determinannya menjadi <InlineMath math="\lambda^n" /> kali determinan semula.

<BlockMath math="\det(\lambda A) = \lambda^n \cdot \det A" />

Hal ini terjadi karena masing-masing dari <InlineMath math="n" /> baris dikalikan dengan <InlineMath math="\lambda" />.

Ketika kita menukar dua baris matriks, determinan berubah tanda:

<BlockMath math="\det B = -\det A" />

Penambahan kelipatan satu baris ke baris lain yang berbeda tidak mengubah determinan:

<MathContainer>
<BlockMath math="\det \begin{pmatrix} \vdots \\ a_j \\ \vdots \\ a_i + \lambda a_j \\ \vdots \end{pmatrix}" />
<BlockMath math="= \det \begin{pmatrix} \vdots \\ a_j \\ \vdots \\ a_i \\ \vdots \end{pmatrix}" />
</MathContainer>

Mengalikan baris dengan skalar <InlineMath math="\lambda \neq 0" /> mengubah determinan menjadi <InlineMath math="\det A' = \lambda \cdot \det A" />. Menukar baris mengubah tanda menjadi <InlineMath math="\det A' = -\det A" />. Menambahkan kelipatan baris lain tidak mengubah determinan sama sekali.

## Kapan Matriks Dapat Dibalik

Determinan menentukan apakah matriks persegi <InlineMath math="A" /> dapat dibalik. Kondisi berikut ekuivalen:

1. **Matriks A dapat dibalik**
2. **Terdapat matriks invers** <InlineMath math="A^{-1}" /> yang memenuhi <InlineMath math="AA^{-1} = I" />
3. **Matriks berperingkat penuh**: <InlineMath math="\operatorname{rank}(A) = n" />
4. **Kernel hanya berisi nol**: <InlineMath math="\ker(A) = \{0\}" />
5. **Kolom-kolom linear independen**
6. **Baris-baris linear independen**
7. **Determinan tidak nol**: <InlineMath math="\det A \neq 0" />

> Jika determinan nol, transformasi memetakan ruang ke himpunan berdimensi lebih rendah, sehingga transformasi tidak dapat dibalik.

Jika baris-barisnya bergantung linear, satu baris merupakan kombinasi linear baris lain. Multilinearitas menguraikan determinan menjadi suku-suku dengan baris berulang, dan setiap suku tersebut nol karena sifat berselang-seling.

## Sifat Perkalian dan Matriks Serupa

Determinan hasil kali dua matriks sama dengan hasil kali determinan masing-masing matriks:

<BlockMath math="\det(AB) = \det A \cdot \det B" />

Jika matriks <InlineMath math="A" /> dapat dibalik, determinan inversnya adalah:

<BlockMath math="\det(A^{-1}) = \frac{1}{\det A}" />

Dua matriks <InlineMath math="A" /> dan <InlineMath math="B" /> disebut **serupa** jika ada matriks dapat diinversi <InlineMath math="S" /> sehingga:

<BlockMath math="B = S^{-1}AS" />

Sifat perkalian determinan membuktikan bahwa matriks yang serupa memiliki determinan yang sama.

<MathContainer>
<BlockMath math="\det B = \det(S^{-1}AS)" />
<BlockMath math="= \det(S^{-1}) \cdot \det A \cdot \det S" />
<BlockMath math="= \frac{1}{\det S} \cdot \det A \cdot \det S = \det A" />
</MathContainer>

Jika eliminasi Gauss hanya memakai penambahan baris dan <InlineMath math="p" /> pertukaran baris, determinan matriks segitiga atas hasil <InlineMath math="R" /> adalah

<BlockMath math="\det R = (-1)^p \cdot \det A" />

Jika suatu baris juga diskalakan, faktor skalanya harus dicatat terpisah. Dengan jenis operasi yang dinyatakan secara jelas, proses membentuk matriks segitiga memberikan algoritma determinan yang praktis.

## Kesalahan Umum tentang Determinan

Determinan **tidak** bersifat aditif:

<BlockMath math="\det(A + B) \neq \det A + \det B" />

Sebagai contoh sederhana:

<MathContainer>
<BlockMath math="\det\left(\begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix} + \begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}\right)" />
<BlockMath math="= \det\begin{pmatrix} 2 & 0 \\ 0 & 2 \end{pmatrix} = 4" />
</MathContainer>

Namun, jika kedua determinan dihitung terpisah, hasilnya:

<MathContainer>
<BlockMath math="\det\begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix} + \det\begin{pmatrix} 1 & 0 \\ 0 & 1 \end{pmatrix}" />
<BlockMath math="= 1 + 1 = 2" />
</MathContainer>

Karena <InlineMath math="4 \neq 2" />, determinan tidak aditif terhadap penjumlahan matriks.
