export const metadata = {
  title: "Transformasi Sumbu Utama",
  description: "Diagonalkan bentuk kuadrat melalui perubahan koordinat ortogonal, lengkapkan kuadrat, dan klasifikasikan irisan kerucut dari nilai eigennya.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2025-07-17",
  dateModified: "2026-08-22",
  subject: "Metode Linear AI",
};

## Bentuk Kuadrat dengan Matriks Simetris

Suku silang dalam persamaan kuadrat dapat menyamarkan bentuk geometrinya. Menulis persamaan sebagai bentuk kuadrat memperlihatkan matriks simetris yang menentukan arah sumbu utamanya. Dalam dua dimensi, misalkan

<MathContainer>
<BlockMath math="A = \begin{pmatrix} a & b/2 \\ b/2 & c \end{pmatrix}" />
</MathContainer>

Karena <InlineMath math="A" /> real dan simetris, matriks tersebut memiliki nilai eigen real <InlineMath math="\lambda_1,\lambda_2" /> serta basis eigen ortonormal <InlineMath math="v_1,v_2" />. Susun vektor eigen itu menjadi <InlineMath math="S=(v_1\quad v_2)" />. Maka <InlineMath math="S^TS=I" /> dan <InlineMath math="S^TAS=\operatorname{diag}(\lambda_1,\lambda_2)" />.

Untuk persamaan <InlineMath math="x^TAx+p^Tx+f=0" />, gunakan koordinat hasil rotasi <InlineMath math="\xi=S^Tx" /> dan koefisien linear hasil rotasi <InlineMath math="(\delta,\epsilon)^T=S^Tp" />. Suku silang pun hilang:

<MathContainer>
<BlockMath math="\lambda_1 \xi_1^2 + \lambda_2 \xi_2^2 + \delta \xi_1 + \epsilon \xi_2 + f = 0" />
</MathContainer>

## Proses Melengkapkan Kuadrat

Untuk kedua variabel dengan <InlineMath math="\lambda_1, \lambda_2 \neq 0" />, proses melengkapkan kuadrat dilakukan secara terpisah:

<MathContainer>
<BlockMath math="\begin{aligned} 0 &= \lambda_1 \left( \xi_1^2 + 2 \frac{\delta}{2\lambda_1} \xi_1 + \frac{\delta^2}{4\lambda_1^2} \right) - \frac{\delta^2}{4\lambda_1} \\ &\quad + \lambda_2 \left( \xi_2^2 + 2 \frac{\epsilon}{2\lambda_2} \xi_2 + \frac{\epsilon^2}{4\lambda_2^2} \right) - \frac{\epsilon^2}{4\lambda_2} + f \end{aligned}" />
</MathContainer>

Setelah suku-sukunya dikumpulkan, kita memperoleh

<MathContainer>
<BlockMath math="\begin{aligned} &= \lambda_1 \left( \xi_1 + \frac{\delta}{2\lambda_1} \right)^2 + \lambda_2 \left( \xi_2 + \frac{\epsilon}{2\lambda_2} \right)^2 \\ &\quad + \left( f - \frac{\delta^2}{4\lambda_1} - \frac{\epsilon^2}{4\lambda_2} \right) \end{aligned}" />
</MathContainer>

Definisikan pusat dalam koordinat hasil rotasi dan konstanta setelah translasi sebagai

<MathContainer>
<BlockMath math="(m_1,m_2)=\left(-\frac{\delta}{2\lambda_1},-\frac{\epsilon}{2\lambda_2}\right)" />
<BlockMath math="\gamma=\frac{\delta^2}{4\lambda_1}+\frac{\epsilon^2}{4\lambda_2}-f" />
</MathContainer>

Dengan demikian,

<MathContainer>
<BlockMath math="\lambda_1 (\xi_1 - m_1)^2 + \lambda_2 (\xi_2 - m_2)^2 - \gamma = 0" />
</MathContainer>

Tanda <InlineMath math="\lambda_1" />, <InlineMath math="\lambda_2" />, dan <InlineMath math="\gamma" /> menentukan apakah himpunan solusi realnya berupa elips, hiperbola, parabola, irisan kerucut degenerat, atau himpunan kosong. Dua kasus berikut mengasumsikan <InlineMath math="\gamma>0" />.

## Klasifikasi Kurva

### Kedua Nilai Eigen Positif

Jika <InlineMath math="\lambda_1 > 0" /> dan <InlineMath math="\lambda_2 > 0" />, maka irisan kerucut yang terbentuk adalah **elips**:

<MathContainer>
<BlockMath math="\frac{(\xi_1 - m_1)^2}{r_1^2} + \frac{(\xi_2 - m_2)^2}{r_2^2} = 1" />
</MathContainer>

Dengan panjang sumbu setengah <InlineMath math="r_1 = \sqrt{\frac{\gamma}{\lambda_1}}" /> dalam arah <InlineMath math="v_1" /> dan <InlineMath math="r_2 = \sqrt{\frac{\gamma}{\lambda_2}}" /> dalam arah <InlineMath math="v_2" />.

<LineEquation
  title={<>Visualisasi Elips dalam Koordinat <InlineMath math="\xi_1, \xi_2" /></>}
  description="Kurva elips dengan kedua nilai eigen positif dan sumbu utama sesuai arah vektor eigen."
  data={[
    {
      points: Array.from({ length: 100 }, (_, i) => {
        const t = (i / 99) * 2 * Math.PI;
        const lambda1 = 2; // λ₁ > 0
        const lambda2 = 1; // λ₂ > 0
        const gamma = 16; // γ > 0
        const m1 = 0; // δ/(2λ₁) = 0
        const m2 = 0; // ε/(2λ₂) = 0
        const r1 = Math.sqrt(gamma / lambda1);
        const r2 = Math.sqrt(gamma / lambda2);
        const x = Math.cos(t) * r1 + m1;
        const y = Math.sin(t) * r2 + m2;
        return { x, y, z: 0 };
      }),
      color: "#0891b2",
      smooth: true,
      showPoints: false
    },
    {
      points: [
        { x: -4, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 }
      ],
      color: "#059669",
      smooth: false,
      showPoints: false,
      labels: [
        {
          text: <InlineMath math="\xi_1" />,
          at: 1,
          offset: [0.3, -0.3, 0]
        }
      ]
    },
    {
      points: [
        { x: 0, y: -3, z: 0 },
        { x: 0, y: 3, z: 0 }
      ],
      color: "#059669",
      smooth: false,
      showPoints: false,
      labels: [
        {
          text: <InlineMath math="\xi_2" />,
          at: 1,
          offset: [0.3, 0.3, 0]
        }
      ]
    },
    {
      points: [
        { x: 0, y: 0, z: 0 }
      ],
      color: "#e11d48",
      smooth: false,
      showPoints: true,
      labels: [
        {
          text: <>Pusat <InlineMath math="(m_1,m_2)" /></>,
          at: 0,
          offset: [0.4, 0.4, 0]
        }
      ]
    }
  ]}
  cameraPosition={[0, 0, 12]}
  showZAxis={false}
/>

### Nilai Eigen Berlawanan Tanda

Ketika <InlineMath math="\lambda_1 > 0" /> dan <InlineMath math="\lambda_2 < 0" />, irisan kerucut yang terbentuk adalah **hiperbola**:

<MathContainer>
<BlockMath math="\frac{(\xi_1 - m_1)^2}{r_1^2} - \frac{(\xi_2 - m_2)^2}{r_2^2} = 1" />
</MathContainer>

Dengan panjang sumbu setengah <InlineMath math="r_1 = \sqrt{\frac{\gamma}{\lambda_1}}" /> dalam arah <InlineMath math="v_1" /> dan <InlineMath math="r_2 = \sqrt{\frac{\gamma}{-\lambda_2}}" /> dalam arah <InlineMath math="v_2" />.

<LineEquation
  title={<>Visualisasi Hiperbola dalam Koordinat <InlineMath math="\xi_1, \xi_2" /></>}
  description="Kurva hiperbola dengan sumbu utama sesuai arah vektor eigen dan pusat transformasi."
  data={[
    {
      points: Array.from({ length: 60 }, (_, i) => {
        const t = (i / 29 - 1) * 2.5;
        const lambda1 = 2; // λ₁ > 0
        const lambda2 = -1; // λ₂ < 0
        const gamma = 3; // γ > 0
        const r1 = Math.sqrt(gamma / lambda1);
        const r2 = Math.sqrt(gamma / (-lambda2));
        const x = Math.cosh(t) * r1;
        const y = Math.sinh(t) * r2;
        return { x, y, z: 0 };
      }),
      color: "#ea580c",
      smooth: true,
      showPoints: false
    },
    {
      points: Array.from({ length: 60 }, (_, i) => {
        const t = (i / 29 - 1) * 2.5;
        const lambda1 = 2;
        const lambda2 = -1;
        const gamma = 3;
        const r1 = Math.sqrt(gamma / lambda1);
        const r2 = Math.sqrt(gamma / (-lambda2));
        const x = -Math.cosh(t) * r1;
        const y = Math.sinh(t) * r2;
        return { x, y, z: 0 };
      }),
      color: "#ea580c",
      smooth: true,
      showPoints: false
    },
    {
      points: Array.from({ length: 2 }, (_, i) => {
        const lambda1 = 2;
        const lambda2 = -1;
        const gamma = 3;
        const r1 = Math.sqrt(gamma / lambda1);
        const r2 = Math.sqrt(gamma / (-lambda2));
        const slope = r2 / r1; // slope asimptot
        const x = (i - 0.5) * 6; // dari -3 ke 3
        const y = slope * x;
        return { x, y, z: 0 };
      }),
      color: "#0284c7",
      smooth: false,
      showPoints: false,
      labels: [
        {
          text: <InlineMath math="r_1" />,
          at: 1,
          offset: [0.5, -0.5, 0]
        }
      ]
    },
    {
      points: Array.from({ length: 2 }, (_, i) => {
        const lambda1 = 2;
        const lambda2 = -1;
        const gamma = 3;
        const r1 = Math.sqrt(gamma / lambda1);
        const r2 = Math.sqrt(gamma / (-lambda2));
        const slope = -r2 / r1; // slope asimptot negatif
        const x = (i - 0.5) * 6; // dari -3 ke 3
        const y = slope * x;
        return { x, y, z: 0 };
      }),
      color: "#0284c7",
      smooth: false,
      showPoints: false,
      labels: [
        {
          text: <InlineMath math="r_2" />,
          at: 0,
          offset: [-0.5, -0.5, 0]
        }
      ]
    },
    {
      points: [
        { x: -4, y: 0, z: 0 },
        { x: 4, y: 0, z: 0 }
      ],
      color: "#7c3aed",
      smooth: false,
      showPoints: false,
      labels: [
        {
          text: <InlineMath math="\xi_1" />,
          at: 1,
          offset: [0.3, -0.3, 0]
        }
      ]
    },
    {
      points: [
        { x: 0, y: -3, z: 0 },
        { x: 0, y: 3, z: 0 }
      ],
      color: "#7c3aed",
      smooth: false,
      showPoints: false,
      labels: [
        {
          text: <InlineMath math="\xi_2" />,
          at: 1,
          offset: [0.3, 0.3, 0]
        }
      ]
    }
  ]}
  cameraPosition={[0, 0, 12]}
  showZAxis={false}
/>

### Salah Satu Nilai Eigen Nol

Kasus khusus terjadi ketika <InlineMath math="\lambda_1\neq0" />, <InlineMath math="\lambda_2=0" />, dan <InlineMath math="\epsilon\neq0" />. Melengkapkan kuadrat memberikan

<MathContainer>
<BlockMath math="0 = \lambda_1 \left( \xi_1^2 + 2 \frac{\delta}{2\lambda_1} \xi_1 + \frac{\delta^2}{4\lambda_1^2} \right) - \frac{\delta^2}{4\lambda_1} + \epsilon \xi_2 + f" />
<BlockMath math="= \lambda_1 \left( \xi_1 + \frac{\delta}{2\lambda_1} \right)^2 + \epsilon \xi_2 + \left( f - \frac{\delta^2}{4\lambda_1} \right)" />
<BlockMath math="= \lambda_1 (\xi_1 - m_1)^2 + \epsilon \xi_2 - \gamma" />
</MathContainer>

Dengan menyelesaikan persamaan terhadap <InlineMath math="\xi_2" />, bentuk parabolanya terlihat langsung:

<MathContainer>
<BlockMath math="\xi_2 = -\frac{\lambda_1}{\epsilon} (\xi_1 - m_1)^2 + \frac{\gamma}{\epsilon}" />
</MathContainer>

<LineEquation
  title={<>Visualisasi Parabola dalam Koordinat <InlineMath math="\xi_1, \xi_2" /></>}
  description="Kurva parabola dengan satu nilai eigen nol dan transformasi sumbu koordinat sesuai vektor eigen."
  data={[
    {
      points: Array.from({ length: 50 }, (_, i) => {
        const t = (i / 49 - 0.5) * 6;
        const lambda1 = 1; // λ₁ ≠ 0
        const epsilon = -2; // ε ≠ 0
        const gamma = 2; // γ
        const m1 = 0; // δ/(2λ₁) = 0
        const x = t;
        const y = -(lambda1 / epsilon) * Math.pow(x - m1, 2) + (gamma / epsilon);
        return { x, y, z: 0 };
      }),
      color: "#9333ea",
      smooth: true,
      showPoints: false
    },
    {
      points: [
        { x: -3.5, y: 0, z: 0 },
        { x: 3.5, y: 0, z: 0 }
      ],
      color: "#0891b2",
      smooth: false,
      showPoints: false,
      labels: [
        {
          text: <InlineMath math="\xi_1" />,
          at: 1,
          offset: [0.3, -0.3, 0]
        }
      ]
    },
    {
      points: [
        { x: 0, y: -0.5, z: 0 },
        { x: 0, y: 4, z: 0 }
      ],
      color: "#0891b2",
      smooth: false,
      showPoints: false,
      labels: [
        {
          text: <InlineMath math="\xi_2" />,
          at: 1,
          offset: [0.3, 0.3, 0]
        }
      ]
    }
  ]}
  cameraPosition={[0, 0, 12]}
  showZAxis={false}
/>

## Contoh Dua Dimensi Terhitung

Perhatikan persamaan

<MathContainer>
<BlockMath math="5x_1^2+4x_1x_2+2x_2^2-6x_1-9=0" />
<BlockMath math="A=\begin{pmatrix}5&2\\2&2\end{pmatrix},\qquad p=\begin{pmatrix}-6\\0\end{pmatrix}" />
</MathContainer>

Pasangan eigennya adalah

<MathContainer>
<BlockMath math="\lambda_1=6,\quad v_1=\frac1{\sqrt5}\begin{pmatrix}2\\1\end{pmatrix},\qquad \lambda_2=1,\quad v_2=\frac1{\sqrt5}\begin{pmatrix}-1\\2\end{pmatrix}" />
<BlockMath math="S=\frac1{\sqrt5}\begin{pmatrix}2&-1\\1&2\end{pmatrix},\qquad \xi=S^Tx" />
</MathContainer>

Pusat dalam koordinat semula adalah <InlineMath math="h=(1,-1)^T" /> karena <InlineMath math="2Ah+p=0" />. Dalam koordinat hasil rotasi, <InlineMath math="m=S^Th=(1/\sqrt5,-3/\sqrt5)^T" />. Dengan <InlineMath math="\eta=\xi-m" />, persamaannya menjadi

<MathContainer>
<BlockMath math="6\eta_1^2+\eta_2^2=12" />
<BlockMath math="\frac{\eta_1^2}{2}+\frac{\eta_2^2}{12}=1" />
</MathContainer>

Kurva tersebut adalah elips yang berpusat di <InlineMath math="(1,-1)" />. Panjang setengah sumbunya adalah <InlineMath math="\sqrt2" /> sepanjang <InlineMath math="v_1" /> dan <InlineMath math="2\sqrt3" /> sepanjang <InlineMath math="v_2" />. Substitusi titik pusat ke persamaan semula menghasilkan <InlineMath math="-12" />, sesuai dengan ruas kanan setelah translasi.

## Permukaan Kuadrat dan Transformasi

Untuk matriks simetris <InlineMath math="A \in \mathbb{R}^{n \times n}" />, vektor <InlineMath math="b \in \mathbb{R}^n" />, dan skalar <InlineMath math="c \in \mathbb{R}" />, **permukaan kuadrat** <InlineMath math="Q" /> didefinisikan sebagai himpunan solusi persamaan kuadrat umum:

<MathContainer>
<BlockMath math="x^T A x + b^T x + c = 0" />
</MathContainer>

Secara ekuivalen,

<MathContainer>
<BlockMath math="Q = \left\{ x \in \mathbb{R}^n : x^T A x + b^T x + c = 0 \right\}" />
<BlockMath math="= \left\{ x \in \mathbb{R}^n : \sum_{j=1}^n \sum_{k=1}^n x_j a_{jk} x_k + \sum_{j=1}^n b_j x_j + c = 0 \right\}" />
</MathContainer>

Jika <InlineMath math="A \in \mathbb{R}^{n \times n}" /> simetris dan <InlineMath math="v_1, \ldots, v_n \in \mathbb{R}^n" /> adalah basis ortonormal dari vektor eigen dengan <InlineMath math="A \cdot v_i = \lambda_i \cdot v_i" />, maka matriks ortonormal <InlineMath math="S = (v_1 \quad \ldots \quad v_n)" /> memungkinkan diagonalisasi <InlineMath math="A \cdot S = S \cdot \Lambda" /> atau <InlineMath math="\Lambda = S^{-1} \cdot A \cdot S = S^T \cdot A \cdot S" />.

Dalam basis koordinat baru <InlineMath math="\xi = S^T x" /> dan <InlineMath math="\mu = S^T b" />, permukaan kuadrat memiliki bentuk diagonal:

<MathContainer>
<BlockMath math="Q = \left\{ \xi \in \mathbb{R}^n : \xi^T S^T A S \xi + b^T S \xi + c = 0 \right\}" />
<BlockMath math="= \left\{ \xi \in \mathbb{R}^n : \xi^T \Lambda \xi + \mu^T \xi + c = 0 \right\}" />
<BlockMath math="= \left\{ \xi \in \mathbb{R}^n : \sum_{j=1}^n \lambda_j \xi_j^2 + \sum_{j=1}^n \mu_j \xi_j + c = 0 \right\}" />
<BlockMath math="= \left\{ \xi \in \mathbb{R}^n : \sum_{j=1}^n (\lambda_j \xi_j^2 + \mu_j \xi_j) = -c \right\}" />
</MathContainer>

Dalam basis ortonormal vektor eigen, bentuk kuadrat memiliki struktur diagonal. Transformasi ini disebut **transformasi sumbu utama** karena sumbu koordinat baru sejajar dengan arah vektor eigen matriks.
