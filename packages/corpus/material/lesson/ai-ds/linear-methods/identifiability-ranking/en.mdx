export const metadata = {
    title: "Identifiability and Matrix Rank",
    description: "Connect parameter identifiability with matrix rank, singular values, conditioning, and stable rank-revealing factorizations.",
    authors: [{ name: "Nabil Akbarazzima Fatih" }],
    date: "2025-07-15",
    subject: "Linear Methods of AI",
};

## Identifiability in a Linear Model

Identifiability asks whether the available data determine a model parameter uniquely. Let <InlineMath math="A \in \mathbb{R}^{m \times n}" />, <InlineMath math="b \in \mathbb{R}^m" />, and <InlineMath math="m \geq n" />. The linear least-squares problem is

<BlockMath math="\min_{x \in \mathbb{R}^n} \|Ax-b\|_2^2" />

and its normal equations are

<BlockMath math="A^T A x = A^T b" />

The parameter vector <InlineMath math="x" /> is uniquely identifiable exactly when <InlineMath math="A" /> has full column rank:

<MathContainer>
<BlockMath math="\operatorname{rank}(A)=n" />
<BlockMath math="\ker(A)=\{0\}" />
</MathContainer>

These statements are equivalent. If <InlineMath math="z \in \ker(A)" /> is nonzero, then

<BlockMath math="A(x+z)=Ax" />

so the data cannot distinguish <InlineMath math="x" /> from <InlineMath math="x+z" />. If the kernel is trivial, <InlineMath math="A^TA" /> is positive definite and the least-squares minimizer is unique.

## Uniqueness Is Not the Same as Stability

Full column rank guarantees a unique exact solution, but not a numerically reliable one. A very small nonzero singular value can make the estimate highly sensitive to small changes in the data.

| Question | Diagnostic | Meaning |
| --- | --- | --- |
| Is the parameter structurally identifiable? | <InlineMath math="\operatorname{rank}(A)=n" /> | No two parameter vectors produce the same prediction. |
| Is the estimate numerically well determined? | <InlineMath math="\sigma_n" /> is not tiny relative to <InlineMath math="\sigma_1" /> | Small perturbations do not cause large parameter changes. |
| How sensitive is the problem? | <InlineMath math="\kappa_2(A)=\sigma_1/\sigma_n" /> | A large condition number signals strong sensitivity. |

This distinction matters in applied models: structural identifiability is an exact algebraic property, while practical identifiability depends on scale, noise, and the numerical tolerance used.

## Rank-Revealing Factorizations

Two common tools expose rank without relying on fragile determinant calculations:

| Method | Strength | Typical use |
| --- | --- | --- |
| QR factorization with column pivoting | Usually cheaper than a full SVD | Fast least-squares solving and practical rank detection |
| Singular value decomposition | Most direct view of all independent directions and their scales | Robust diagnostics near rank deficiency |

Plain LU factorization is primarily a square-system tool. For a rectangular design matrix, pivoted QR or SVD is the more natural rank-revealing choice.

## Singular Value Decomposition

Let <InlineMath math="q=\min(m,n)" />. Every matrix <InlineMath math="A \in \mathbb{R}^{m \times n}" /> has a singular value decomposition

<BlockMath math="A=U\Sigma V^T" />

where <InlineMath math="U \in \mathbb{R}^{m \times m}" /> and <InlineMath math="V \in \mathbb{R}^{n \times n}" /> are orthogonal, and <InlineMath math="\Sigma \in \mathbb{R}^{m \times n}" /> has nonnegative diagonal entries

<BlockMath math="\sigma_1 \geq \sigma_2 \geq \cdots \geq \sigma_q \geq 0" />

The exact rank is the number of positive singular values:

<BlockMath math="\operatorname{rank}(A)=\#\{i:\sigma_i>0\}" />

For <InlineMath math="m \geq n" />, full column rank is therefore equivalent to <InlineMath math="\sigma_n>0" />. Right singular vectors associated with zero singular values span <InlineMath math="\ker(A)" /> and reveal parameter directions that the data cannot identify.

## Exact Rank and Numerical Rank

If the exact rank is <InlineMath math="r" /> with <InlineMath math="0<r<q" />, then

<BlockMath math="\sigma_r>0=\sigma_{r+1}=\cdots=\sigma_q" />

Floating-point data rarely contain exact zeros. A numerical rank instead counts singular values above a scale-aware threshold <InlineMath math="\tau" />:

<MathContainer>
<BlockMath math="\tau=c\,\max(m,n)\,\varepsilon_{\mathrm{mach}}\,\sigma_1" />
<BlockMath math="\operatorname{rank}_{\tau}(A)=\#\{i:\sigma_i>\tau\}" />
</MathContainer>

The factor <InlineMath math="c" /> reflects the application and noise model, so there is no universal decimal cutoff. If <InlineMath math="A" /> is rank deficient, the least-squares minimum still exists, but the minimizing parameter vector is not unique. The Moore-Penrose pseudoinverse selects the minimum-norm solution:

<BlockMath math="x^{\dagger}=V\Sigma^{\dagger}U^T b" />

## Computing the Decomposition Safely

The identity

<BlockMath math="A^TA=V\Sigma^2V^T" />

explains why squared singular values are eigenvalues of <InlineMath math="A^TA" />. It is useful for theory, but explicitly forming <InlineMath math="A^TA" /> squares the condition number:

<BlockMath math="\kappa_2(A^TA)=\kappa_2(A)^2" />

Numerical software therefore uses dedicated SVD or rank-revealing QR algorithms rather than forming <InlineMath math="A^TA" /> merely to estimate rank. This preserves more information in the directions that are already close to unidentifiable.
