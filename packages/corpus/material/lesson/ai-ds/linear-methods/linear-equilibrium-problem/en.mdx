export const metadata = {
  title: "Linear Least-Squares Problem",
  description: "Solve inconsistent linear systems with least squares, residual norms, orthogonal projection, normal equations, and numerically stable methods.",
  authors: [{ name: "Nabil Akbarazzima Fatih" }],
  datePublished: "2025-07-15",
  dateModified: "2026-08-22",
  subject: "Linear Methods of AI",
};

## From an inconsistent system to a best fit

Let <InlineMath math="A \in \mathbb{R}^{m \times n}" />, <InlineMath math="x \in \mathbb{R}^n" />, and <InlineMath math="b \in \mathbb{R}^m" />. If <InlineMath math="b" /> does not belong to the column space of <InlineMath math="A" />, the system

<BlockMath math="Ax=b" />

has no exact solution. This commonly happens when a model is fitted to noisy measurements or when an overdetermined system has more independent equations than unknowns.

When exact equality is unavailable, define the residual

<BlockMath math="e(x)=Ax-b" />

and choose the vector whose residual is smallest. The standard linear least-squares problem is

<BlockMath math="\hat{x}\in\operatorname*{arg\,min}_{x\in\mathbb{R}^n}\|Ax-b\|_2^2" />

Because the residual has <InlineMath math="m" /> components, the objective expands to

<BlockMath math="\|Ax-b\|_2^2=\sum_{i=1}^{m}(Ax-b)_i^2" />

Squaring makes every contribution nonnegative and penalizes a large residual component more strongly than a small one.

## The geometric condition

The vector <InlineMath math="A\hat{x}" /> is the point in <InlineMath math="\operatorname{col}(A)" /> closest to <InlineMath math="b" /> in Euclidean distance. At the optimum, the residual is orthogonal to every column of <InlineMath math="A" />:

<MathContainer>
<BlockMath math="A^{\mathsf T}(A\hat{x}-b)=0" />
<BlockMath math="A^{\mathsf T}A\hat{x}=A^{\mathsf T}b" />
</MathContainer>

The second equation is called the **normal equation**.

<LineEquation
  title="Orthogonal projection in a least-squares problem"
  description={<>The blue vector ends at <InlineMath math="A\hat{x}" />, the orange vector ends at <InlineMath math="b" />, and the purple vector is the residual <InlineMath math="A\hat{x}-b" />.</>}
  data={[
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 4, y: 2, z: 1 },
        { x: 6, y: 3, z: 1.5 }
      ],
      color: "#0284c7",
      cone: { position: "end" },
      showPoints: false,
      labels: [
        { text: <InlineMath math="A\hat{x}" />, at: 2, offset: [0.5, 0.5, 0] }
      ]
    },
    {
      points: [
        { x: 0, y: 0, z: 0 },
        { x: 2.5, y: 2, z: 1.5 },
        { x: 5, y: 4, z: 3 }
      ],
      color: "#d97706",
      cone: { position: "end" },
      showPoints: false,
      labels: [
        { text: <InlineMath math="b" />, at: 2, offset: [0.5, 0, 0.5] }
      ]
    },
    {
      points: [
        { x: 5, y: 4, z: 3 },
        { x: 5.5, y: 3.5, z: 2.25 },
        { x: 6, y: 3, z: 1.5 }
      ],
      color: "#9333ea",
      cone: { position: "end" },
      showPoints: false,
      labels: [
        { text: <InlineMath math="A\hat{x}-b" />, at: 1, offset: [0, -0.5, 0] }
      ]
    }
  ]}
/>

The scene shows the target, its projection, and the connecting residual. The blue vector represents the projected point in the column space; it is not a drawing of the entire subspace. The orthogonality condition above is the exact statement that makes <InlineMath math="A\hat{x}" /> the nearest point.

## A small worked example

Suppose one constant <InlineMath math="x" /> must fit the three observations <InlineMath math="1" />, <InlineMath math="2" />, and <InlineMath math="6" />. Then

<BlockMath math="A=\begin{pmatrix}1\\1\\1\end{pmatrix},\qquad b=\begin{pmatrix}1\\2\\6\end{pmatrix}" />

and the objective is

<MathContainer>
<BlockMath math="F(x)=(x-1)^2+(x-2)^2+(x-6)^2" />
<BlockMath math="F'(x)=2(x-1)+2(x-2)+2(x-6)=6x-18" />
</MathContainer>

Setting the derivative to zero gives <InlineMath math="\hat{x}=3" />. The fitted vector and residual are

<MathContainer>
<BlockMath math="A\hat{x}=\begin{pmatrix}3\\3\\3\end{pmatrix}" />
<BlockMath math="A\hat{x}-b=\begin{pmatrix}2\\1\\-3\end{pmatrix}" />
</MathContainer>

The residual components sum to zero, so

<BlockMath math="A^{\mathsf T}(A\hat{x}-b)=2+1-3=0" />

as required by the normal equation.

## Other residual norms

The norm determines what kind of error the fit prioritizes.

| Objective | What it controls |
| --- | --- |
| <InlineMath math="\displaystyle\min_x\|Ax-b\|_2^2=\min_x\sum_{i=1}^{m}(Ax-b)_i^2" /> | Penalizes large residuals quadratically and gives the usual least-squares projection. |
| <InlineMath math="\displaystyle\min_x\|Ax-b\|_1=\min_x\sum_{i=1}^{m}\lvert(Ax-b)_i\rvert" /> | Uses a linear penalty, so an occasional large residual has less influence than under squared loss. |
| <InlineMath math="\displaystyle\min_x\|Ax-b\|_\infty=\min_x\max_{1\le i\le m}\lvert(Ax-b)_i\rvert" /> | Minimizes the worst absolute residual. |

The <InlineMath math="\ell_1" /> and <InlineMath math="\ell_\infty" /> formulations can be rewritten as linear programs. Their suitability still depends on the data-generating process and the error criterion that matters for the application.

## Solving least squares reliably

If <InlineMath math="A" /> has full column rank, the least-squares minimizer is unique. The normal equation then implies

<BlockMath math="\hat{x}=(A^{\mathsf T}A)^{-1}A^{\mathsf T}b" />

This identity is useful for analysis, but explicitly forming the inverse is not the preferred numerical algorithm. In practice:

- use a QR factorization for a stable full-rank least-squares solve
- use an SVD when rank deficiency or ill-conditioning matters
- use the pseudoinverse to select the minimum-norm solution when minimizers are not unique

There is also a precise statistical interpretation. In the model

<BlockMath math="b=Ax_*+\varepsilon,\qquad \varepsilon\sim\mathcal{N}(0,\sigma^2I)" />

with fixed <InlineMath math="A" /> and independent Gaussian errors of common variance, least squares is the maximum-likelihood estimator of <InlineMath math="x_*" />. Correlated errors or errors with unequal variance require weights that reflect their covariance.
