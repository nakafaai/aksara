export const metadata = {
  title: "Polinomial Karakteristik",
   description: "Polinomial karakteristik digunakan untuk mencari nilai eigen, menentukan multiplisitas aljabar, serta menghubungkan koefisien polinomial dengan jejak matriks.",
    authors: [{ name: "Nabil Akbarazzima Fatih" }],
    datePublished: "2025-07-12",
    dateModified: "2026-09-01",
    subject: "Metode Linear AI",
};

## Polinomial Karakteristik dari Determinan

Sebuah nilai eigen <InlineMath math="\lambda" /> dari matriks persegi <InlineMath math="A" /> memiliki vektor eigen tak nol <InlineMath math="v" /> yang memenuhi

<BlockMath math="Av=\lambda v" />

Dengan menyusun ulang persamaan, kita memperoleh <InlineMath math="(A-\lambda I)v=0" />. Solusi tak nol ada tepat ketika <InlineMath math="A-\lambda I" /> singular, sehingga determinannya harus nol. Polinom karakteristik mencatat syarat ini untuk semua skalar yang mungkin sekaligus.

Misalkan <InlineMath math="A \in \mathbb{K}^{n \times n}" />. Kita dapat membentuk fungsi khusus:

<MathContainer>
<BlockMath math="\chi_A(t) = \det(A - t \cdot I)" />
<BlockMath math="\chi_A(t) = a_n \cdot t^n + a_{n-1} \cdot t^{n-1} + \cdots + a_1 t + a_0" />
</MathContainer>

Polinom berderajat <InlineMath math="n" /> dalam <InlineMath math="t" /> dengan koefisien <InlineMath math="a_0,\ldots,a_n\in\mathbb{K}" /> ini disebut **polinom karakteristik** dari <InlineMath math="A" />. Polinom karakteristik ditulis dengan konvensi <InlineMath math="\det(A-tI)" />. Konvensi <InlineMath math="\det(tI-A)" /> juga sering digunakan. Kedua polinom hanya berbeda dengan faktor <InlineMath math="(-1)^n" />, sehingga akar kedua polinom tetap sama.

## Jejak Matriks dan Koefisien Polinomial

Misalkan <InlineMath math="A \in \mathbb{K}^{n \times n}" /> adalah matriks persegi. **Jejak** dari <InlineMath math="A" /> adalah jumlah dari elemen-elemen diagonal:

<BlockMath math="\operatorname{tr}(A) = \sum_{i=1}^n a_{ii}" />

Jejak matriks memiliki hubungan erat dengan koefisien polinomial karakteristik.

### Hubungan Koefisien dengan Jejak dan Determinan

Dalam polinomial karakteristik <InlineMath math="\chi_A(t)" /> dari <InlineMath math="A" />, koefisien-koefisiennya memiliki makna khusus:

<BlockMath math="a_n = (-1)^n, \quad a_{n-1} = (-1)^{n-1}\operatorname{tr}(A), \quad a_0 = \det A" />

Ini berarti:
- Koefisien tertinggi selalu <InlineMath math="(-1)^n" />
- Koefisien kedua tertinggi terkait dengan jejak matriks
- Suku konstanta adalah determinan matriks

## Nilai Eigen sebagai Akar Polinomial

Nilai <InlineMath math="\lambda \in \mathbb{K}" /> merupakan nilai eigen dari <InlineMath math="A" /> jika dan hanya jika <InlineMath math="\det(A - \lambda \cdot I) = 0" />.

Jadi, **nilai eigen merupakan akar dari polinomial karakteristik**.

Untuk <InlineMath math="t \in \mathbb{K}" />, perhatikan persamaan berikut.

<BlockMath math="\det(A - t \cdot I) = 0" />

Persamaan ini disebut **persamaan karakteristik** dari <InlineMath math="A" />.

## Multiplisitas Aljabar

Sekarang, bagaimana jika sebuah nilai eigen muncul beberapa kali sebagai akar polinomial karakteristik? Misalkan <InlineMath math="A \in \mathbb{K}^{n \times n}" /> dan <InlineMath math="\lambda \in \mathbb{K}" />. Multiplisitas dari akar <InlineMath math="t = \lambda" /> dari polinomial karakteristik <InlineMath math="\chi_A(t)" /> disebut **multiplisitas aljabar** <InlineMath math="\mu_A(\lambda)" /> dari nilai eigen <InlineMath math="\lambda" /> dari <InlineMath math="A" />. Kita katakan <InlineMath math="\lambda" /> adalah nilai eigen dengan multiplisitas <InlineMath math="\mu_A(\lambda)" /> dari <InlineMath math="A" />.

### Batasan Multiplisitas

Untuk setiap nilai eigen <InlineMath math="\lambda" />, multiplisitas aljabarnya memenuhi

<BlockMath math="1 \leq \mu_A(\lambda) \leq n" />

## Hubungan Multiplisitas Geometrik dan Aljabar

Multiplisitas geometrik dan aljabar memenuhi batas berikut.

Untuk nilai eigen <InlineMath math="\lambda" /> dari <InlineMath math="A \in \mathbb{K}^{n \times n}" />, multiplisitas geometrik dan aljabarnya memenuhi

<BlockMath math="1 \leq \dim \operatorname{Eig}_A(\lambda) \leq \mu_A(\lambda) \leq n" />

> Multiplisitas geometrik setiap nilai eigen selalu lebih kecil atau sama dengan multiplisitas aljabarnya.

Multiplisitas geometrik menghitung banyaknya vektor eigen bebas dalam ruang eigen, sedangkan multiplisitas aljabar menghitung berapa kali akar tersebut muncul dalam polinom karakteristik. Bentuk Jordan menunjukkan bahwa setiap vektor eigen bebas memerlukan setidaknya satu blok Jordan, sehingga jumlah geometrik tidak dapat melebihi jumlah aljabar.

## Menghitung Polinomial Karakteristik

Pada bagian berikut, kita memakai definisi di atas. Hubungan antarkoefisien dipakai untuk memeriksa hasil perhitungan.

### Matriks Orde Tiga

Misalkan <InlineMath math="A = \begin{pmatrix} 3 & 2 & -1 \\ 1 & 0 & -4 \\ 3 & 0 & 1 \end{pmatrix} \in \mathbb{K}^{3 \times 3}" />. Polinomial karakteristik dari <InlineMath math="A" /> adalah:

<MathContainer>
<BlockMath math="\chi_A(t) = \det \begin{pmatrix} 3-t & 2 & -1 \\ 1 & -t & -4 \\ 3 & 0 & 1-t \end{pmatrix}" />
<BlockMath math="= (3-t)(-t)(1-t) - 2(13-t) - 3t" />
<BlockMath math="= -3t + 3t^2 + t^2 - t^3 - 2 + 2t - 24 - 3t" />
<BlockMath math="= -t^3 + 4t^2 - 4t - 26" />
</MathContainer>

Untuk <InlineMath math="\mathbb{K} = \mathbb{R}" />, <InlineMath math="\chi_A(t)" /> hanya memiliki akar <InlineMath math="\lambda_1 \approx -1{,}8003" /> dengan multiplisitas aljabar <InlineMath math="\mu_A(\lambda_1) = 1" />.

Untuk <InlineMath math="\mathbb{K} = \mathbb{C}" />, <InlineMath math="\chi_A(t)" /> memiliki akar <InlineMath math="\lambda_1 \approx -1{,}8003" />, <InlineMath math="\lambda_2 \approx 2{,}9001 + 2{,}4559i" />, dan <InlineMath math="\lambda_3 \approx 2{,}9001 - 2{,}4559i" /> dengan multiplisitas aljabar masing-masing <InlineMath math="\mu_A(\lambda_1) = \mu_A(\lambda_2) = \mu_A(\lambda_3) = 1" />.

Akar tersebut lolos dua pemeriksaan berguna: jumlahnya sama dengan jejak matriks, sedangkan hasil kalinya sama dengan determinan.

<MathContainer>
<BlockMath math="\lambda_1+\lambda_2+\lambda_3=4=\operatorname{tr}(A)" />
<BlockMath math="\lambda_1\lambda_2\lambda_3=-26=\det(A)" />
</MathContainer>

### Nilai Eigen Berulang pada Matriks Segitiga Atas

Polinomial karakteristik dari matriks <InlineMath math="A = \begin{pmatrix} 1 & 2 \\ 0 & 1 \end{pmatrix}" /> adalah:

<MathContainer>
<BlockMath math="\chi_A(t) = \det(A - t \cdot I) = \det \begin{pmatrix} 1-t & 2 \\ 0 & 1-t \end{pmatrix}" />
<BlockMath math="= (1-t) \cdot (1-t) - 0 \cdot 2 = (1-t)^2" />
<BlockMath math="= t^2 - 2 \cdot t + 1" />
</MathContainer>

Matriks ini memiliki akar <InlineMath math="\lambda = 1" /> dengan multiplisitas aljabar <InlineMath math="\mu_A(1) = 2" />. <InlineMath math="\lambda = 1" /> adalah satu-satunya nilai eigen dari <InlineMath math="A" />. Kita telah menghitung bahwa <InlineMath math="\dim \text{Eig}_A(1) = 1" />.

## Polinomial Karakteristik Transformasi Geometrik

Pemetaan yang umum pada <InlineMath math="\mathbb{R}^2" /> berikut menghubungkan perhitungan aljabar dengan makna geometrisnya.

### Rotasi

Rotasi dengan <InlineMath math="A = \begin{pmatrix} \cos(\alpha) & -\sin(\alpha) \\ \sin(\alpha) & \cos(\alpha) \end{pmatrix}" /> memiliki polinomial karakteristik:

<BlockMath math="\chi_A(t) = (\cos(\alpha) - t)^2 + \sin(\alpha)^2 = t^2 - 2 \cdot \cos(\alpha) \cdot t + 1" />

Diskriminannya adalah <InlineMath math="-4\sin^2(\alpha)" />. Akarnya real tepat ketika <InlineMath math="\sin(\alpha)=0" />, sehingga, dalam satu putaran penuh, <InlineMath math="\alpha\equiv0" /> atau <InlineMath math="\alpha\equiv\pi\pmod{2\pi}" />.

### Refleksi

Refleksi terhadap sumbu <InlineMath math="x" /> dengan <InlineMath math="A = \begin{pmatrix} 1 & 0 \\ 0 & -1 \end{pmatrix}" /> memiliki polinom karakteristik:

<BlockMath math="\chi_A(t) = (1-t) \cdot (-1-t) = t^2 - 1" />

Nilai eigen adalah <InlineMath math="\lambda_1 = 1" /> dan <InlineMath math="\lambda_2 = -1" /> dengan <InlineMath math="\mu_A(1) = \mu_A(-1) = 1" />.

### Peregangan

Peregangan dengan <InlineMath math="A = \begin{pmatrix} s & 0 \\ 0 & s \end{pmatrix}" /> memiliki polinomial karakteristik:

<BlockMath math="\chi_A(t) = (s-t)^2 = t^2 - 2 \cdot s \cdot t + s^2" />

Nilai eigen adalah <InlineMath math="\lambda = s" /> dengan <InlineMath math="\mu_A(s) = 2" />.

### Geseran

Geseran dengan <InlineMath math="A = \begin{pmatrix} 1 & s \\ 0 & 1 \end{pmatrix}" /> dengan <InlineMath math="s \neq 0" /> memiliki polinomial karakteristik:

<BlockMath math="\chi_A(t) = (1-t)^2 = t^2 - 2 \cdot t + 1" />

Nilai eigen adalah <InlineMath math="\lambda = 1" /> dengan <InlineMath math="\mu_A(1) = 2" />.

## Sifat Matriks Serupa

Matriks serupa menggambarkan transformasi linear yang sama dalam basis berbeda. Karena itu, keduanya memiliki polinom karakteristik, nilai eigen, jejak, dan determinan yang sama.

Misalkan <InlineMath math="S \in \mathbb{K}^{n \times n}" /> dapat diinversi dan <InlineMath math="B = S^{-1} \cdot A \cdot S" />. Maka:

<MathContainer>
<BlockMath math="\chi_B(t) = \det(B - t \cdot I) = \det(S^{-1} \cdot A \cdot S - t \cdot S^{-1} \cdot I \cdot S)" />
<BlockMath math="= \det(S^{-1} \cdot (A - t \cdot I) \cdot S)" />
<BlockMath math="= \det S^{-1} \cdot \det(A - t \cdot I) \cdot \det S = \chi_A(t)" />
</MathContainer>

### Sifat Vektor Eigen Matriks Serupa

Vektor eigen berubah mengikuti basisnya. Jika <InlineMath math="v \in \mathbb{K}^n" /> adalah vektor eigen <InlineMath math="A" /> untuk <InlineMath math="\lambda" />, maka <InlineMath math="w = S^{-1}v" /> adalah vektor eigen <InlineMath math="B" /> untuk nilai eigen yang sama.

Dengan menggunakan <InlineMath math="Av=\lambda v" /> dan <InlineMath math="w=S^{-1}v" />, kita memperoleh

<MathContainer>
<BlockMath math="B \cdot w = S^{-1} \cdot A \cdot S \cdot S^{-1} \cdot v = S^{-1} \cdot A \cdot v" />
<BlockMath math="= S^{-1} \cdot \lambda \cdot v = \lambda \cdot S^{-1} \cdot v = \lambda \cdot w" />
</MathContainer>

Jadi, keserupaan mempertahankan nilai eigen, sedangkan invers perubahan basis mengubah vektor eigen yang bersesuaian.
